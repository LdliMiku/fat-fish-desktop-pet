# 大肥鱼桌宠开发文档

本文档是后续开发的交接入口。每次开发完成后追加记录，并同步修订发生变化的架构、素材映射和操作说明。项目根目录 `AGENTS.md` 要求后续开发者遵守这一流程。

## 最新交接：四角独立尺寸（修正一端宽一端窄）、角落装饰不再被拉伸（2026-09-20）

**最新一轮：① 用户指出"靠近角色的一端更宽、远离端更窄"——根因是上一版把四角尺寸按"整行/整列取最大值"计算（右下角为了包住鲸鱼被撑到 1010 像素，于是左右两端圆角大小不同、看着一端宽一端窄）。② 用户反馈"右上角的爱心崩了"，爱心同样落在被拉伸的切片里。修正：`.build/prepare-bubble-ui.cjs` 改为**四个角各自独立尺寸**——只在对应角落的窗口里量出装饰范围（左上小笔触、右上爱心、右下鲸鱼＋尾巴），把那个角单独放大，其余角仍用素材自身圆角大小；可见圆角来自素材本身，所以角大小不同不会再造成一边宽一边窄。元数据里 `corner` 由 `{left,right,top,bottom}` 改成 `{tl,tr,bl,br}`（每个 `{w,h}`），新增 `edge:{w,h}` 表示四边拉伸条的厚度；`src/PetBubble.cs` 按四角独立尺寸重写九宫格布局（先画四边与中心、再画四角压住接缝），并保留"每片向外多取 2 像素"的防接缝逻辑。本素材实测四角：左上 376×209、右上 376×237、左下 113×113、右下 771×353。检查通过，已部署（08:49:55）。**

**最新一轮（按用户逐条反馈）：① 文字要"上下左右都居中"——原来只是按左右不对称的 padding 居中，视觉偏左；现在改用气泡中线：`contentWidth = max(文字宽, 宽度 −(padLeft+padRight))`、`textLeft = (宽度 − contentWidth)/2`。② "气泡边缘不够圆润、爱心变形"——上一版压平填色时把"接近底色的内部像素"一律替换，误伤了爱心内部的浅色和描边附近的抗锯齿过渡；现在改为先按"与底色通道差 >14"识别墨色像素、再向外膨胀 3 像素做保护掩膜，只压平掩膜之外的纯底色（压平像素数从 86 万降到 53 万），爱心、鲸鱼、圆角过渡都保持原样。③ "白底看不出细线、桌面上能看出"——根因是九宫格切片边界落在非整数像素上，拉伸切片在边缘做线性采样时混入了透明像素，在深色桌面上表现为一条淡灰线；修复分两步：几何取整到整像素（`leftW/rightW/topH/bottomH` 与气泡宽高都 `Math.Round`），并让每条切片向外多取 2 像素（目标矩形同比例外扩，见 `Slice` 里的 Bleed 逻辑），这样切片边缘的像素始终是邻居的内容，采样混色也混不到背景。深色背景（#2B4A52）预览已确认细线消失。④ 聊天界面"贴太紧"：日志区留白 6→10，她的气泡再加 6 像素左边距、用户的加 6 像素右边距，文字与窗口边界保持距离。检查 21 项通过，已部署。**

**最新一轮改动（按用户逐条反馈）：① 聊天窗口顶部的大标题「和大肥鱼说话」删除，消息直接贴顶；② 聊天里她那一侧的手绘气泡改为镜像版（尾巴朝左，原来朝右是反的）；③ 聊天里用户那一侧的气泡文字也改居中；④ 气泡内边距收紧（`.build/prepare-bubble-ui.cjs` 改为 `left = corner.left×scale×0.9+4`、`right = min(corner.right,260)×scale×0.9+6`、`top = corner.top×scale×0.7+3`、`bottom = corner.bottom×scale×0.45+4`，本素材实测 14/29/11/20，两行文字的气泡高度从 86 降到 73）；⑤ 用户截图指出气泡内部有横竖两条细线——那是九宫格切片边界：中间/边缘被拉伸而四角保持原样，纸纹密度在交界处对不上；现在素材脚本会把"接近底色（通道差 ≤18）"的内部像素压成同一底色（实测约 86 万像素，颜色 #f6f4f3），只保留描边、装饰与柔和边缘，接缝消失（底边一行填充色极差从 25 降到 10）。检查 21 项通过，已部署。**

**最新改动：用户要求"聊天界面布局优化一下，左边空太多"，并希望"聊天的气泡回复也用上面的（手绘气泡）"。现在 `src/PetApp.cs` 的 `AddChatBubble` 里，她那一侧直接调用 `PetBubble.BuildContent(text)` 复用手绘气泡素材（和桌面弹出的气泡完全同一套视觉），用户那一侧保持浅蓝圆角气泡并把最大宽度从 320 放宽到 400；窗口内边距 14→10、日志区留白 8→6、消息间距 8→4，整体更贴边。新增 `.build/ChatWindowMock.cs` 与 `.build/make-chat-mock.ps1`，用和程序相同的控件结构渲染聊天窗口截图，便于以后检查布局。已部署替换。**

**最新修正：用户指出"尾巴没闭合"，随后要求文字在气泡里居中。原因是上一版把尾巴和鲸鱼抠成独立贴图、并把本体缺口修补成直边，于是尾巴的描边和气泡底边断开。现在改为：本体直接使用原图（不做任何修补），把右下角切片放大到把尾巴和整块鲸鱼装饰（鲸鱼＋水花＋小爱心，实测 721×298）都包进去——它们在同一个"不缩放"的切片里，天然连成一体、也不会被拉伸；同时删掉独立尾巴/装饰贴图的绘制。文字改为水平 + 垂直居中：宽度取"文字宽度"和"可用宽度"中较大者并设 `TextAlignment=Center`，纵向按实际高度均分留白（短句在偏宽的气泡里也居中）。检查全部通过，已部署。**

**最新修正：用户反馈"气泡少了标志性小鲸鱼、尾巴也稀碎"，并要求把没做出来的对话转头逻辑删掉。排查结论：手绘素材里的小鲸鱼那一块（连同水花与小爱心）宽度约 721 像素，远大于当时的四角切片（113 像素），于是落进了会被横向拉伸的中间区域，被拉成几乎看不见；尾巴贴图本身只有 122×44 像素，在 scale 0.1 下只有 12×4 像素，所以看着"稀碎"。修正：① `.build/prepare-bubble-ui.cjs` 增加"内部装饰块"检测——在右下角窗口内按与行填充色的差异找出成片装饰，抠成独立贴图（本素材实测 721×298），本体对应区域用镜像补齐，装饰以固定尺寸叠在右下角，不再被拉伸；② 尾巴仍独立贴图并固定在朝向角色那一侧的底角；③ 删除转头逻辑（`PetSpriteView.ForceLook`、`PetApp` 的延时显示与 `bubble.Hidden` 恢复全部移除）；④ 气泡更贴近角色：角色可见范围按 14% 内缩后再留 4 像素间隙。检查 21 项全部通过，已部署。**

**最新改动：用户要求换掉尾巴跟随逻辑、让气泡出现在角色侧边（像手绘参考图）、大小随文本量变化、尾巴位置固定、左右按"离桌面哪边边缘远"决定，并在触发气泡前朝气泡那一侧转头且保持到气泡消失。现在 `src/PetBubble.cs` 去掉尾巴滑动，新增 `PlacementFor(角色矩形, 气泡尺寸, 工作区)`：气泡出现在离边缘更远的一侧，竖向让尾巴尖对齐角色高度的 42% 并夹进工作区，镜像时整体水平翻转（文字不翻转），尾巴固定在朝向角色的底角。`PetSpriteView.ForceLook(x)` 让视线优先于鼠标跟随，`PetApp.ShowTalkLine` 先判断气泡将落在哪侧立即转头、320 毫秒后才弹出，`bubble.Hidden` 时清空强制视线——转头持续到气泡消失。素材换成用户给的手绘版（纸纹填充、双层描边、右上爱心、右下鲸鱼），`prepare-bubble-ui.cjs` 适配了手绘轮廓：尾巴按"连续下凹 >12 像素且成片 ≥24 列"识别、底边线用 8 像素分箱众数、外挂装饰自动检测（这版鲸鱼在气泡内部所以 `shell: null`）、内边距与文字宽度重新推导。检查全部通过，已部署。**

**最新修复（为明天换素材铺路）：① 尾巴贴图原来连"底边线以上 10 像素"一起切进去，贴回本体时和修好的底边对不齐，边缘显示成一条竖线，尾巴一动线就跟着动（用户看到"放到左边线就跑到左边"）；现在切片上边界改为底边线本身，新增检查"底边上方无垂直接缝"（修复前填充色极差 67、修复后 3）。② 气泡高度原来被四角切片撑到最小 64 DIP，一句话也很高；现在 `.build/prepare-bubble-ui.cjs` 按"描边+白圈厚度 ≈ 3 DIP"自动算出 `scale`（当前参考图 0.086），高度改为 `max(2×角高+6, 上留白+文字高+下留白)`，一行 48、两行 69、三行 90，完全跟着文字走。③ 新增 `assets/ui/bubble-meta.json`（由素材脚本自动量出几何、缩放、内边距、尾巴与右下角装饰、文字颜色），`src/PetBubble.cs` 通过 `BubbleSkin.Load()` 读取，读取失败退回默认值——明天换素材只需覆盖 `bubble-source.png` + 重跑脚本 + 重编译，不用改代码。④ 气泡整体朝屏幕中心偏移最多 18% 宽度，尾巴因此落在偏左/偏右的位置，肉眼能看出跟随（原来始终居中、尾巴停在正中间）。检查 20 项全部通过，预览图按"角色在 22%／50%／82% 位置"输出。**

**最新美术接入：用户提供参考图（GPT 生成：淡蓝圆角气泡 + 蓝描边 + 白内圈 + 左上高光 + 右下角小鲸鱼 + 底部尖尾巴）。`.build/prepare-bubble-ui.cjs` 把参考图拆成三份运行素材——`assets/ui/bubble-body.png`（去掉尾巴的凹口、并用镜像把鲸鱼覆盖的区域补成完整圆角，接缝处按行统一填色）、`bubble-tail.png`（独立的尾巴贴图）、`bubble-whale.png`（右下角鲸鱼贴图）；原始参考图保存为 `assets/ui/bubble-source.png`。`src/PetBubble.cs` 改为九宫格绘制：四角按原始比例、四边与中心拉伸，所以文字变长时只有中段变形；鲸鱼和尾巴作为独立贴图叠加。用户点的"尾巴跟随"已实现——尾巴水平位置由 `TailLeftFor(角色中轴, 气泡左边缘, 气泡宽度)` 计算，夹在两端直边内，角色在左/右时尾巴相应偏移。`.build/check-bubble.ps1` 用真实 WPF 渲染路径检查 19 项并输出 `.build/bubble-preview.png`（三张预览分别模拟角色在左/中/右），全部通过；已部署替换。**

**最新功能：用户要求"让她主动发一些话"。新增 `src/PetTalk.cs`——纯本地规则的搭话引擎（不联网、零成本、断网可用）：启动 4 秒后按时段（早上／下午／晚上／深夜）问候；约 45 分钟没有互动则提醒休息（两次提醒至少隔 1 小时）；按可调间隔（默认 30 分钟、范围 5–90 分钟、实际 0.6–1.4 倍浮动）主动闲聊，每天上限 16 次；任何两条主动台词之间至少隔 3 分钟；摸头／点击／拎起／落地会即时接一句（点她有 45% 概率不说话，互动回应之间隔 12 秒）。全部通过头顶气泡显示。右键新增「主动搭话」开关与「搭话间隔…」面板，设置键 `talkEnabled`／`talkInterval`。`.build/check-talk.ps1` 用假时钟跑 29 项检查全部通过；已部署替换。**

**最新功能：用户要求"加上交流气泡，不需要点进交流界面就能看见她说了什么"。新增 `src/PetBubble.cs`——独立透明置顶窗口，圆角粉色气泡 + 底部尾巴，配色与对话窗口里她那一侧一致；文字越长显示越久（`2.2 + 0.16 × 字数`，夹在 3–14 秒），0.12 秒淡入、0.3 秒淡出，新消息到达时替换内容并重新计时。100 毫秒跟随定时器让气泡跟着角色移动与缩放（角色缩放时窗口尺寸不变，只靠 Window 事件会跟丢），位置按工作区夹紧，点气泡直接打开对话窗口。`src/PetApp.cs` 发送消息时先显示「……」，回复到达后替换为正文，失败时显示"唔……这次没答上来。"；右键菜单新增「显示对话气泡」开关（设置键 `bubbleEnabled`，默认开启）。新增 `.build/check-bubble.ps1` 用真实 WPF 渲染路径检查并输出 `.build/bubble-preview.png`，14 项全部通过；已部署替换。**

**最新修正（已获用户实机确认）：用户最初反馈"感觉和之前没什么区别"。先证明部署无误——把运行中 `大肥鱼桌宠.exe` 的内嵌 `UnifiedSprites` 资源解出来与磁盘图集比对，SHA256 完全一致，说明新图集确实在跑；再用新增的 `.build/measure-frame-chroma.cjs`（按 B>R+20／R>B+10 把像素分成蓝／肤／白三组）复测，发现上一轮只统一了整体亮度，**蓝色像素本身仍相差很大**：蓝通道 131.6–141.7（约 10），而蓝色是画面主体，这就是"颜色不一样"的来源。现在 `.build/prepare-v6.cjs` 的色调统一升级为逐帧、逐通道的"基础偏移 + 蓝度加权偏移"，同时满足"整体均值对齐正视帧"和"蓝色像素均值对齐正视帧"两道约束（解 2×2 方程求 base／extra），蓝度权重 `clamp((B−R−20)/40,0,1)` 平滑过渡以免在阈值处出现色块边界，单像素总偏移限制 ±10。结果各组整体 R112.0–113.1／G120.6–121.8／B149.6–151.2（原先 R111.1–118.0／G119.4–125.1／B147.0–154.2），蓝色像素 R71.7–75.3／B133.4–138.8（原先 R69.5–76.4／B131.6–141.7），`atlas.json` 仍逐字节未变。已重新导出原生帧、重算运动场并替换程序；用户随后回复"可以的确实好多了"，确认跳变感明显减轻。**

**最新修正：用户要求把各动画的颜色统一，减少切换时的颜色跳变。`.build/measure-frame-groups.cjs` 逐帧测量发现各来源组的平均亮度相差最多 5.7：正视 121.5、基础／左右下方向 125.6、闭眼桥接 121.0、上方完整 124.1、上方半程 124.4、左右下半程 120.6、摸头 126.3。现在 `.build/prepare-v6.cjs` 在合成图集后自动做一次色调统一：逐帧统计不透明像素平均亮度，以正视帧（第 0 帧）为基准计算修正量（单帧限制 ±8），对 RGB 施加同一偏移（alpha、几何、注册数据完全不动），再写入 `unified-sheet.png`。结果 59 帧全部落在 121.4–121.8，平均修正量 3.2，`atlas.json` 与修改前逐字节相同。只统一亮度、不动色相，避免把姿态不同的肤色／布料比例强行拉平。已重新导出原生帧、重算运动场并替换程序。**

**最新功能：用户选定"接云 API + 聊天记录存本地"，并确认使用 DeepSeek 接口。新增 `src/PetChat.cs`（配置／加密 Key／历史／请求）与 `src/PetApp.cs` 里的对话窗口和接口设置面板；右键菜单新增「和我说话…」。默认接口 `https://api.deepseek.com/v1`、模型 `deepseek-chat`，API Key 用 DPAPI 加密存 `data/chat-key.dat`（不写明文、不进 settings.txt），聊天记录存 `data/chat-history.jsonl`，请求只带最近 8 轮上下文。任何失败（无 Key／401／超时／断网）都退回本地台词并在窗口底部说明原因。新增 `.build/check-chat.ps1` 用本地模拟接口做离线检查，41 项全部通过；真实 Key 的联网效果与窗口手感待用户实机确认。语音朗读、语音输入、回复带动画按用户要求留到后续。**

**最新素材修正：用户反馈“正视图的蓝色比其他动画亮，出入有点大”，要求把正视图蓝色调低一点。`.build/measure-direction-tone.cjs` 逐帧测量确认：正视帧 0／8 的蓝色像素（B>R+20）为 R77.5／B139.5、整帧 115.1／121.9／150.6，而用户手改三张图时没有波及的左侧／右侧／下方方向（帧 19、21–24、37–41）蓝色像素约 R72.5／B137.7、整帧约 112.5／120.9／149.8。新增 `.build/prepare-front-tone.cjs`，按“越蓝的像素压得越多”的逐通道权重降低正视图第一格 RGB（不动 alpha、不动第二三格），结果正视帧变成 112.0／120.9／149.6、蓝色像素 73.6／138.0，与其他朝向基本一致。只影响帧 0／8，其余 57 帧逐像素与注册标定不变。程序已重建替换。**

**最新功能：用户要求把待机左右转头的间隔做成 10–30 秒可调。原「张望频率（少／标准／多）」已取消，改为 `IdleLook.Interval` 单一数值，范围 10–30 秒、默认 20 秒；每次实际等待在设定值附近 ±12% 浮动并强制留在 10–30 秒内（避免机械感）。右键菜单新增「调节张望间隔…」，面板复用 NumericSettingRow，滑块按整秒吸附、支持数值输入、即时生效并自动保存（键名 `idleLookInterval`）。旧设置 `idleLookFrequency` 自动换算：少 → 30 秒、标准 → 20 秒、多 → 15 秒，且新键存在时不被旧键覆盖。间隔是真实时间，与「左右张望」倍率独立。`.build/KeyframeChecks.cs` 的 IdleLookChecks 已按新模型更新并通过；旧 HeadPetChecks 仍为已知过期断言。程序已编译并替换，用户设置迁移已验证（`idleLookFrequency=1` → `idleLookInterval=20`）。**

**最新发布：已把这一版实际用到的内容整理进私有仓库 `LdliMiku/fat-fish-desktop-pet` 的 `main` 分支，提交 `fac0ecf`（父提交 `0ac9848`），本地工作副本为 `C:\chatgpt\大肥鱼桌宠-release`（原 `C:\chatgpt\大肥鱼桌宠-v0.9` 已不存在，本次重新克隆）。入库内容为程序、`build.ps1`、`src/`（含新增 `PetParticles.cs`）、运行素材（`assets/animations/v6`、新增 `head-pet-manual-sheet.png`、新增 `assets/particles/`）、素材脚本、`tests/` 与文档；已排除用户设置 `data/settings.txt`、`_archive/` 备份、`.build` 缓存与候选程序、`demo/`。克隆目录内重新编译成功，产物大小与推送的程序一致（35,216,896 字节）。未打新 tag，程序集版本仍为 0.1.0.0。**

**最新素材接入：用户自行调节了摸头九宫格（`assets/animations/v6/head-pet-manual-sheet.png`，20:50:48 保存）后要求接入。九格整体再次提亮，平均 R+2.4～+2.7／G+1.9～+2.1／B+1.85～+1.94，每格约 41–44% 的不透明像素有变化，最大通道差 29–40；九格 alpha 变化像素均为 0，轮廓、剪影与位置完全未动，画布仍为 1218×1291。已重新生成图集、导出 59 帧原生位图、重算 157 对运动场并替换程序。核对通过：只有帧 50–58 变化，其余 50 帧像素与标定完全一致。色调读回：待机正视亮度 123.1，摸头 f50／f55／f52／f58 分别为 125.1／126.1／126.8／125.5，即手改后摸头比待机亮约 2.0–3.7（手改前两者相差 0.1）。这是用户手动选择的结果，是否符合预期由用户实机判断。**

**最新修复（含用户要求的二次校正）：用户反馈“怎么摸头的色调变这么深了？”。逐帧测量显示摸头专用帧 50–58 在最近三个版本中逐字节完全相同，真正变化的是底图正视帧（亮度由 119.1 升到 123.1），使摸头保持姿势相对待机的色调差从 2.8 拉大到 5.9；原因是摸头九宫格仍按用户第一次改图之前的底图色调生成。先按用户自己的编辑变换（逐通道线性拟合 R²≈0.995）重出 `head-pet-tone-matched-sheet.png`，再按用户“让摸头与待机色调完全一致”的要求追加一级常量偏移（等效 R 0.9838x+8.89、G 0.9953x+7.23、B 1.0062x+6.06）。全程只改 RGB，未碰 alpha、姿势、轮廓与几何。摸头校准帧 f50 亮度 120.1 → 123.0，与待机正视 f0（123.1）相差 0.1，其余摸头姿势差距 ≤2.1。是否协调仍以用户实机观感为准。**

**最新素材接入：用户在 20:24–20:27 又细调了 `bridges/16.png`、`17.png`、`18.png`，本轮重新生成图集与过渡数据、重新编译并替换本地桌宠。核对通过：只有帧 0、8、16、17、18、34、35、36 使用新绘画，其余 51 帧逐像素与标定不变，画布与格子尺寸未变。相对上一版，三张图只是轻微压暗（人物内部平均 R−1.4／G−0.8／B+0.1、R−1.8／G−1.0／B+0.1、R−4.4／G−2.6／B+0.3），轮廓 alpha 完全没有变化。观感是否达到预期仍以用户实机体验为准。**

**最新素材接入：用户手动清理 `bridges/16.png`、`17.png`、`18.png` 的溢色后，上一轮只重新生成了图集与过渡数据，没有构建和替换程序；本轮续做，重新编译并替换了本地桌宠。图集核对通过：只有帧 0、8、16、17、18、34、35、36 使用新绘画，其余 51 帧逐像素与标定不变，三组完整／半程标定一致。数值测量显示新版三张图相对旧版整体略亮（人物内部平均约 R+6／G+5／B+4），轮廓、比例、位置和画布尺寸未变；若用户本意只是去发梢溢色，这层提亮可能是编辑工具带来的，可回滚。是否接受以用户实机体验为准。**

**最新用户反馈纠正上一版结论：溢色仍有少量、头发色差未解决，新生成的上方三姿势又引入体型大小变化。现已撤下 `upward-hair-fixed.png` 的运行引用，改用现有 `bridges/16.png`、`17.png`、`18.png` 第三格作为完整姿势；分别与各自第二格半程共用第一格正视的 scaleHeight／widthFactor 标定，不再使用新生成五联图的比例。18 仍跳过旧头顶补丁。三组标定一致、其余 56 帧像素与标定未变已检查，重新导出并计算运动场；数值检查不等于发色／残边／体型观感已验收。**

**最新修复：用户报告左上／正上／右上发梢越界残色、头顶颜色偏深。三个完整姿势 16–18 原先沿用 v5 素材，与 v6 正视和半程不同源，且 18 又覆盖旧头顶补丁。本轮用 imagegen 基于原姿势、以现有正视色调为参考修整发梢和上部发色，接入 `upward-hair-fixed.png` 的第 2–4 格；18 跳过旧头顶补丁。已重新导出原生帧并计算 157 对运动场。上方朝向专项 39 组渲染检查通过（透明像素格式与画布边界），静态对照的残边和色差改善；实机切换尚待用户验收。摸头与爱心未修改。**

**最新退出要求覆盖此前“直接放手”：用户反馈退出生硬，现增加约 0.65 秒（1 倍）的退出过渡。停止有效抚摸超过 0.65 秒真实时间后，按 55 → 52 → 51 → 0 回放现有实体中间姿势，让双手逐步放下、表情恢复，再衔接原有正视停稳与跟随恢复。退出时停止新增爱心，已有爱心继续自然消散；持续摸头与贴图爱心不变。实际衔接自然度待用户体验。**

**最新实现覆盖下方矢量方案：用户要求生成简单柔和的爱心图像，再套用粒子逻辑。本轮用内置 imagegen 生成透明背景粉色手绘爱心，素材为 `assets/particles/pink-heart.png`，原图和提示同目录保存。`src/PetParticles.cs` 已移除运行时心形曲线、填色与描边，改为缓存并绘制该 PNG；随机位置、环绕上升、律动、渐隐、独立倍率与持续摸头均沿用。生成与编译不代表视觉验收。**

**最新反馈修正：用户看到爱心中间有竖线、边缘不柔和。源码中原凹口两段曲线在同一点反向竖直折返，且使用默认尖角描边，是内向尖刺的可能来源。本轮将凹口改为圆滑二次曲线，底尖也圆化，两层描边明确使用 Round 连接，主边线由 0.10 减至 0.065，并降低描边与渐变对比。保留粉色、环绕升起、醒目大小与持续摸头逻辑；尚未做实机视觉确认。**

**最新要求与实现：爱心改为用户参考图中的淡粉心形、柔和玫粉边线，取消蓝色珠光配色和玻璃高光；保留醒目大小、角色两侧随机位置、泡泡般环绕上升与淡出。摸头已改为持续保持：完成一次进入动作后固定胸前手势与笑脸，头部区域内持续有效横向抚摸就保持；停止有效移动约 0.65 秒后直接放手，鼠标静止不续期。交互逻辑和视觉效果尚待用户实机体验。**

### 当前开发进度

- 当前本地程序已取消摸头时的左右转头，仅保留开心弯眼、微笑、淡腮红与双手收在胸前的变化；素材仍含轻低头姿态。进入顺序为 0 → 51 → 52 → 55，随后持续保持 55。
- 当前源码 `src/PetAnimation.cs` 的进入过程在 1 倍下约 0.80 秒，之后将动作进度停在 0.80 并固定显示 55；持续有效抚摸刷新真实时间续期，不重新播放收手。停止有效移动约 0.65 秒后进入软退出（进入过程尚未结束时先完成进入），按 55 → 52 → 51 → 0 回放现有实体中间姿势放开双手并恢复表情，再用正视停稳与跟随渐入交接，不再直接跳回 0。
- 原左右轻蹭的 54／56 不再参与播放，避免轻蹭时后发及分片线随姿势切换。`src/PetMotionWarp.cs` 中之前的局部固定处理保留，但当前摸头保持阶段直接使用单帧。
- 当前本地程序于 2026-09-19 20:30 使用用户二次细调后的 `bridges/16–18.png` 重新生成的图集与运动场编译替换，备份 `_archive/before-manual-hair-details-20260919-202750`（上一轮 20:18 版为 `_archive/before-manual-hair-20260919-194605`）；保留用户设置、GitHub 平滑缩放修复与全部独立倍率（含 HeadPet、摸头爱心）。独立 v0.9 仓库未同步。
- 编译和部署属于制作进度，不代表视觉验收。按用户此前要求未做摸头动画检查；肤色、手势自然度、进入／退出衔接，以及本次三张图整体提亮是否接受，均待用户体验，历史变宽与残留反馈不能据此宣称已解决。

### 目标与进度（粉色爱心与持续摸头均已接入，待用户体验）

1. **持续摸头就不退出动作。** 用户持续进行有效的摸头操作时，保持开心表情和胸前双手，不因固定时长结束而放手，也不循环重播收手动作。停止有效抚摸后才进入结束、直接放手并恢复正常跟随。持续判断沿用头部区域内的有效抚摸采样，不把鼠标仅停留不动视为一直在摸；允许短暂采样间隙以避免来回换向时闪退，当前宽限时间为 0.65 秒真实时间。开始识别、持续维持和停止判定分开；已有冷却不能中断已开始的持续摸头，拖动等明确中断仍正常生效。
2. **摸头过程中增加随机位置爱心粒子。** 在角色周围预设的局部区域随机生成小爱心，避开脸部、眼睛、双手和重要发束分界线；每枚粒子出生位置只随机一次，之后连续运动，不逐帧重新随机导致跳动。
3. **爱心从下往上升，并逐渐消失。** 爱心持续向上飘动，可带极轻的横向漂移；随生命周期降低透明度，最终移除。摸头持续期间少量、间隔生成，停止或中断后不再产生新爱心，已有爱心自然上升淡出，不突然整批消失。
4. 爱心与现有蓝色系角色协调，采用柔和淡粉、小尺寸、低密度，不遮挡表情，不满屏堆叠。限制同时存活数量，持续摸很久或降低粒子速度也不能无限累积。具体大小、生成间隔、数量、速度与寿命为后续制作参数，尚未确定或验收。
5. 粒子使用独立绘制层，不改变角色肤色、头发绘画或体型，不恢复已取消的转头。采用角色局部坐标，随移动和缩放同步，不扩大鼠标命中区、不抢焦点。保留现有平滑缩放与用户设置。
6. 沿用统一调速约定：爱心效果在原“调节动画速度…”面板内增加独立 0.25–5 倍倍率，默认 1 倍并保存；旧设置缺项补默认值。持续抚摸识别、停止宽限及粒子生成上限采用真实时间，与动画倍率分开。

实现入口建议：在 `src/PetAnimation.cs` 接入有效抚摸续期与保持／退出状态；可新增 `src/PetParticles.cs` 管理粒子生成、生命周期与绘制，通过 `src/PetApp.cs` 和 `src/AnimationRates.cs` 接入渲染、独立倍率和设置，并按需要更新 `build.ps1`。优先完成持续摸头和摸头爱心，再考虑待机微粒、问号和感叹号。

### 后续开发约束与验证边界

- 保持已确认的颜色、体型、头发留白、呆毛方向；角色每个画面只采样一套完整绘画，不做整图透明叠化。爱心自身淡出独立于角色绘画。
- 用户此前明确要求“不要检查，我自己检查就行”：摸头任务不主动运行自动动画回归、截图或实机视觉验收；必要制作、编译与部署可正常完成，记录时与用户体验分开。未经后续授权不把检查扩展回来。
- 本轮已接入参考图粉色爱心与持续摸头保持；不恢复转头。以上目标覆盖早期“固定时长摸头／每轮仅一组爱心／先做待机粒子”的提案，历史过程保留在文末记录中。

## 当前状态

- Windows WPF 桌宠，使用 .NET Framework 4.x；当前发布版本 v0.2.0（tag `v0.2.0`），程序集版本 0.2.0.0。
- 支持点击、拖动、拎起/放下、八方向鼠标跟随、五秒鼠标静止后回正、眨眼和呼吸。
- 本地左右张望：默认开启，张望间隔可在 10–30 秒之间调节（默认 20 秒，右键 →「调节张望间隔…」，整秒吸附、可输入秒数、即时生效并保存）；复用 v6 原有转头姿势。专项断言（IdleLookChecks）已通过，用户体验待验收。
- 当前摸头为双手胸前、开心弯眼、轻低头固定姿势（已取消左右扭动），进入约 0.80 秒（1 倍），持续有效摸头时保持，停止有效移动约 0.65 秒后按 55 → 52 → 51 → 0 软退出放手；结束后正视停稳 0.18 秒，再用普通转头倍率控制的 0.4 秒目标渐入恢复跟随。摸头期间按独立“摸头爱心”倍率生成淡粉贴图爱心。未做摸头检查，变宽、抽搐及旧版蓝色残留是否消失尚未确认。
- 素材版本：2026-09-19 23:42 替换的程序使用用户二次细调后的 `bridges/16.png`、`17.png`、`18.png` 生成图集与运动场；`bridges/17.png` 第一格（正视图）另经 `.build/prepare-front-tone.cjs` 做蓝度加权下调；生成图集后由 `.build/prepare-v6.cjs` 自动把全部 59 帧亮度统一到正视水平（组间差 5.7 → 0.4），坐标与注册数据不变。摸头专用九宫格由用户手改主文件 `head-pet-manual-sheet.png` 驱动。轮廓、画布与注册尺寸未变，观感待用户确认。
- 摸头色调：用户已自行调整九宫格色调（20:50:48 版，整体提亮约 R+2.4／G+2.0／B+1.9，alpha 未动），现已接入。当前待机正视亮度 123.1，摸头姿势 125.1–126.8，即摸头比待机亮约 2.0–3.7，为用户的主动选择；如需要可回滚到两者完全一致的 20:45 版本。
- 已移植 GitHub 5b688c5 的固定透明窗口与平滑缩放：滚轮、滑块、数值输入共用平滑目标尺寸，缩放时固定脚底锚点。缩放期间暂停摸头手势采样，避免局部坐标变化被误判为轻扫；缩放专项自动检查与实机体验分别记录。
- 右键菜单支持连续尺寸调节、数值输入，以及各类动画独立倍率。
- 对话：右键 →「和我说话…」打开对话窗口，走 DeepSeek 的 OpenAI 兼容接口（默认 `https://api.deepseek.com/v1` + `deepseek-chat`）。API Key 用 DPAPI 加密存 `data/chat-key.dat`，接口与模型存 `data/chat-config.txt`，聊天记录存 `data/chat-history.jsonl`；失败时退回本地台词。语音朗读、语音输入、回复联动动作尚未实现。
- 说话气泡：她回话时会在角色头顶显示粉色气泡（`src/PetBubble.cs`），不需要打开对话窗口；显示 3–14 秒后淡出，跟着角色移动与缩放，点气泡打开对话窗口，右键「显示对话气泡」可关闭（设置键 `bubbleEnabled`）。主动搭话尚未实现。
- 主动搭话：`src/PetTalk.cs` 让她自己开口——启动与跨时段问候、45 分钟无互动提醒休息、按 5–90 分钟（默认 30）可调间隔随机闲聊（每天最多 16 次、任意两句至少隔 3 分钟）、摸头／点击／拎起／落地即时回应。全部是本地台词，不联网；右键「主动搭话」开关、「搭话间隔…」调频率。
- 用户已确认实体中间帧版本转头顺滑，并同意将统一正视后的当前版本作为 v0.1 上传。
- 开发工作目录：`C:\chatgpt\大肥鱼桌宠`。先前整理的 Git 仓库位于 `C:\chatgpt\大肥鱼桌宠-v0.9`，不要默认两个目录已同步。远程为私有仓库 `LdliMiku/fat-fish-desktop-pet`。

## 代码入口

### 长期约定：所有动作统一支持独立调速（用户于 2026-09-18 指定）

- 以后每个新增动作都必须有独立的播放倍率，统一加入原有右键“调节动画速度…”面板，不另设分散的调速入口，不隐式借用其他动作的倍率。
- 统一支持滑块、数值输入、0.25–5 倍范围、默认 1 倍、即时生效、设置保存与“全部恢复 1 倍”。旧设置文件缺少新动作时只补默认值，保留原有设置。
- 触发间隔、识别阈值和冷却使用真实时间，与动作播放倍率分开。动作进行中修改倍率应从当前进度继续，不重播或跳帧。
- 扩展 `AnimationKind` 时在末尾追加，同步维护 `AnimationRates.Names`、默认值、持久化与回归检查；速度面板及重置逻辑按动作数量生成，禁止写死七项等固定数量。复合动作的自身转头/眼部等阶段服从该动作倍率，正常鼠标跟随和眨眼保留自己的倍率。
- 完成新动作的标准包含其速度项可见、有效、可保存及旧设置兼容；文档须记录验证结果与用户体验反馈。

| 文件 | 职责 |
| --- | --- |
| `src/PetApp.cs` | 透明窗口、鼠标交互、菜单、设置持久化及进程节流设置 |
| `src/PetAnimation.cs` | 动作状态、鼠标方向、帧加载注册、眼睛遮罩及 WPF 绘制 |
| `src/PetMotionWarp.cs` | 运动场读取、相邻姿势几何插值、单源画面渲染 |
| `src/PetSizeMotion.cs` | 固定缩放画布、尺寸平滑、脚底锚点和可见范围边界（GitHub 5b688c5） |
| `src/AnimationRates.cs` | 各类动画倍率及持久化 |
| `src/NumericSettingRow.cs` | 菜单滑块和数值输入 |
| `src/PetChat.cs` | 对话配置、DPAPI 加密的 API Key、聊天记录文件、DeepSeek 请求与本地兜底台词 |
| `src/PetBubble.cs` | 桌面说话气泡窗口：显示时长、跟随角色位置、点击打开对话窗口 |
| `src/PetTalk.cs` | 主动搭话规则：时段问候、久坐提醒、随机闲聊、互动回应与冷却（纯本地台词） |
| `build.ps1` | 使用系统 C# 编译器构建并嵌入资源 |

## 动画与素材

当前构建使用 `assets/animations/v6`，59 帧。`unified-sheet.png` 为图集，`atlas.json` 保存裁切、缩放和脚底水平注册位置；`motion-fields.gz` 为由最终原生帧计算的双向位移场。

| 帧号 | 用途 |
| --- | --- |
| 0 | 当前正视 |
| 1–15 | 历史基础动作、眨眼、拎起和落地；2 是正视闭眼，8 是正视副本 |
| 16、17、18、19、21、22、23、24 | 左上、上、右上、左、右、左下、下、右下 |
| 20 | 旧方向表中的正视，当前跟随使用 0 |
| 25–33 | 原方向闭眼素材，方向开放帧加 9 得到对应闭眼帧 |
| 34–41 | 八方向半程实体姿势，顺序同方向列表 |
| 42–49 | 半程闭眼素材，开放帧加 8 |
| 50 | 新摸头素材的正常站姿校准帧；不替换现有待机原画 |
| 51、52 | 双手收向胸前、弯眼微笑与低头的进入中间姿势 |
| 53、54 | 双手胸前、低头向左轻扭的完整、半程姿势 |
| 55 | 双手胸前、低头弯眼微笑的中央姿势 |
| 56、57 | 双手胸前、低头向右轻扭的半程、完整姿势 |
| 58 | 双手放下、睁眼回正的恢复中间姿势 |

`bridges/16.png` 等八张三联图保留生成原图；每张的中间角色用于半程姿势。当前正视取 `bridges/17.png` 第一格。完整上方姿势 16–18 使用对应桥接图第三格，与第二格共用同一 neutral.h 和宽度系数。`upward-hair-fixed.png` 因用户反馈体型漂移停用，仅留历史。`bridge-blinks.png` 是对应闭眼九宫格，中心格提供正视闭眼眼部。`top-repairs.png/json` 在加载时仅修复 21、24；18 使用同源完整绘画，跳过旧补丁。

素材由内置图像生成工具制作。核心提示词约束：保持既有二维角色、低饱和蓝色、体型、线条、服装、头发留白与呆毛方向；三联图为正视、准确半程、目标姿势，透明背景、脚底对齐。闭眼素材只要求改变眼睑和紧邻眼睛的像素，保持原姿势和布局。程序仍以眼部遮罩隔离生成图的其他差异。

摸头当前使用 `head-pet-manual-sheet.png` 专用九宫格（3×3，1218×1291，每格 406×430，左上到右下依次为帧 50–58）。这是**用户可手改的主文件**：它由 `head-pet-tone-matched-sheet.png`（色调校正产物）复制而来，内容相同；`.build/prepare-v6.cjs` 只读该文件，故手改后重跑图集即可生效，**不要再运行 `.build/prepare-head-pet-tone.cjs`**，否则会用原始素材覆盖手改结果。历史素材 `head-pet-original-tone-sheet.png`（第一次改图前的色调版本）、`head-pet-chest-sheet.png`、`head-pet-sheet.png`、`head-pet-fixed-sheet.png`、`head-pet-yaw-strip.png` 均不再进入图集；提示词与映射见 `head-pet-chest-generation.md`。整组使用正常站姿（第一格）的同一个高度尺度与固定宽度系数，不按低头/转头帧另行放大或水平拉伸，运行中仍以脚底注册；格子尺寸或角色位置大幅改动会改变裁切与注册结果。摸头表情不经过旧闭眼遮罩，普通眨眼逻辑保留。按用户要求未执行摸头视觉验收。

## 渲染规则与已解决问题

1. 曾经对整张姿势做颜色/透明度混合，造成头发、尾巴和面部双轮廓。现在 `RenderWeighted` 只采样权重最大的一个源绘画，其他权重只影响坐标变换。
2. 单源切换最初跳动明显，现加入八个真实半程姿势。跟随权重沿正视、半程、完整方向组成两层方向网格。
3. `BlinkPixels` 只在 `EyeMask` 内插值眼睛颜色；眼睛外逐字节保留当前开放姿势，透明度始终保留。闭眼不得换掉整个人物。
4. 非激活窗口曾明显卡顿，已禁用进程执行速度节流，保留正常进程优先级；渲染并行度限制为 4。用户确认后台流畅度明显改善。
5. 运动场以半分辨率保存和采样，避免为每一对姿势扩展整幅场占用大量内存。

## 构建与验证

仅代码变更：运行 `powershell -ExecutionPolicy Bypass -File .\build.ps1`。

修改 v6 原始素材时，顺序执行：

1. Node 运行 `.build/prepare-v6.cjs`，重新生成图集和坐标。
2. PowerShell 运行 `.build/check-native-animation.ps1 export`，导出最终 WPF 像素到 `.build/motion-inputs`。
3. Python 运行 `.build/analyze-v6-motion.py`，重新计算位移场。
4. PowerShell 运行 `.build/check-native-animation.ps1`，执行动作、倍率、遮罩和单源绘制检查。
5. 查看 `demo/animation-qa/v10-switch-audit.png` 的切换点，以及方向和眨眼对照；再构建可执行文件并实机确认。

动画重建脚本位于 `.build`。`prepare-v6.cjs` 优先使用常规 Node `sharp` 包，本机可回退到 Codex 自带依赖；`analyze-v6-motion.py` 需要 Python、NumPy 和 OpenCV。运行程序及普通源码构建不需要这些开发依赖。测试的离屏耗时不等于桌面实际帧率。`FormattedText` 构造函数过时警告为现存警告，不阻止编译。

## 生动化开发路线（左右张望、摸头回应已实现，其余为提案）

目标：让角色在没人操作时也有自然的小行为，在被互动时给出有区别的回应。保留当前已确认的外观、动作幅度和可调速功能，以安静陪伴为主。

### 首选：主动小动作

建议先做一个小规模的“自主待机行为”功能：安静站着一会儿后，她偶尔看看左右，或短暂歪头发呆，再自然回到正视。动作之间留出足够的安静时间，不连续循环同一段表演。

- 第一阶段只做“左右张望”：优先复用当前正视、左右半程和完整转头姿势，先验证自主行为与现有鼠标跟随是否衔接自然。
- 初始节奏建议：距离上次与桌宠互动至少 20 秒后才允许触发，每次再随机等待 20–45 秒；张望约 2–4 秒，完成后重新计时。同一动作避免连续重复。以上为待实机调整的初值，不代表用户已确认。
- 鼠标移动时优先保留现有跟随；仅在鼠标静止、角色已回正且无交互时开始自主动作。鼠标重新移动时平滑退出张望并恢复跟随。这里判断的是鼠标及桌宠交互，不把它解释为用户离开电脑。
- 点击、拖动、拎起、落地及打开菜单期间暂停自主动作；结束后重新等待，不立即补播。点击和拖动优先级高于自主动作，避免抢动作或叠加变形。
- 菜单提供“自主小动作”开关和“少 / 标准 / 多”频率选项；触发频率与动画播放速度分开。首版张望继续使用现有转头倍率，不改变已有倍率语义；新增设置向后兼容旧设置文件。
- 第二阶段再加入歪头、轻轻晃尾巴等新动作。先检查现有历史帧的外观是否与当前正视一致；无法满足时补绘完整关键帧与中间姿势，不直接拿旧素材拼接。

### 后续候选

| 功能 | 体验构想 | 开发重点 |
| --- | --- | --- |
| 摸头回应 | 鼠标在头部轻轻来回移动一小段时间，她眯眼或轻歪头回应 | 头部命中区随缩放变化；设置触发阈值与冷却；与按下拖动区分；眯眼只改眼部，新歪头补绘实体姿势 |
| 打瞌睡与唤醒 | 长时间没与她互动时，偶尔眼皮变沉、点头；点击后清醒 | 首版做短暂瞌睡，不因鼠标静止就判断用户离开；设计入睡、被打断和恢复姿势，避免拖动时仍睡着 |
| 点击的不同回应 | 偶尔歪头、轻晃尾巴，替代每次完全相同的反馈 | 当前点击为整体轻缩放；新反应要补齐素材与过渡，连续点击限频，不堆积动作 |

推荐顺序：左右张望 → 摸头回应 → 打瞌睡。用户已选择并实现左右张望，下面的时间构想保留为设计背景，实际行为以本节实现说明和最新开发记录为准；其余候选尚未实现。

### 左右张望：当前实现

- 默认开启，标准频率为安静约 40–65 秒后开始；“少”为 60–110 秒，“多”为 30–42.5 秒。鼠标持续移动、交互或菜单打开时重新等待，不累积待播动作。完成一轮、关闭菜单或恢复交互后重新计时。
- 一轮先看一边、再看另一边、最后回正；首个方向随机，之后交替。1 倍时约 4 秒，动作时钟使用独立的“左右张望”倍率，等待间隔不受倍率影响。回到正视并稳定后才算完成。
- 点击、按住、拎起、落地、鼠标移动、菜单及设置面板会中断或暂停张望；恢复跟随时复用原有阻尼追随，保留当前位置和速度。渲染间隔超过 1 秒时取消旧动作并重新等待，避免恢复后补播。
- 右键新增“自主小动作（左右张望）”和“张望频率”。`data/settings.txt` 新增 `idleLookEnabled`、`idleLookFrequency`；旧设置缺少这两项时使用开启和标准频率，原有位置、尺寸、置顶与七类倍率保留。
- 没有改动素材、运动场或单源渲染代码，无须重导原生素材或重算运动场。

### 实现边界与预计涉及文件

- `src/PetAnimation.cs`：接入自主行为的计时、选择与过渡，协调现有 `Idle / PickingUp / Lifted / Landing` 状态；继续遵守单源绘画与眼部遮罩规则。
- `src/PetApp.cs`：传递交互和菜单状态，增加开关、频率选项及设置持久化；保留旧位置、尺寸和动画倍率。
- `src/AnimationRates.cs`：仅在新增动作确实需要独立倍率时扩展，同时检查菜单中当前按七类动画枚举的逻辑；当前已追加 IdleLook、HeadPet 独立倍率类别。
- 素材阶段涉及 `assets/animations/v6` 与 `.build` 的导出、运动场及验证流程；实际开发时先备份，再决定扩展现有素材集还是另建版本。
- 实现后同步更新 `使用说明.md` 和本文档；通过验证后才替换运行程序，并按约定正常退出旧版、备份可执行文件。

### 验证与验收计划

- 自动验证：使用可控时间与随机种子验证冷却、开关、暂停恢复和连续输入；覆盖张望中点击、拎起、落地、鼠标重新移动，以及旧设置加载和新增设置保存。确认原有倍率、五秒回正、单源绘制和眨眼遮罩规则没有退化。
- 素材有变更时：重新导出原生帧、计算运动场、执行原生渲染检查，并查看动作开始、打断和结束处的画面对照。
- 人工体验验收：实机观察是否过于频繁、重复、突兀，能否及时响应拖动，后台运行是否仍顺滑；由用户判断是否“更生动但不打扰”。自动检查通过不等于用户已验收。
- 待解决：左右张望的用户实机体验及频率偏好；其他候选的具体表现。左右张望自动验证结果见最新记录，尚无用户体验验收。

## 周身微粒与情绪表达效果设计（摸头爱心已接入，其他待实现，2026-09-19）

本节根据用户要求先记录设计，不代表已经开发或获得视觉验收。整体延续大肥鱼的灰蓝色头发、奶白色服饰细节和圆润二维画风：粒子像身边偶尔浮起的小水光，情绪符号像角色自己的小表情。保持轻、慢、少，优先让人注意到角色的眼神和动作。以下数量、尺寸、时长和触发场景均为制作初值或提案，后续根据体验调整。

### 待机周身粒子：安静的小水光

在头发外侧、肩旁、裙摆及尾巴周围稀疏出现小圆点、空心小泡泡，偶尔混入一枚圆角四芒小星。以灰蓝、浅冰蓝、奶白为主，边缘柔和，最多带一圈很淡的光晕；不使用高饱和霓虹、大面积辉光、频繁闪烁或连续喷射。粒子缓缓浮起、轻微横漂后自然淡出，错开发生，不排成明显光环，也不围着角色高速旋转。

以角色高 300 像素为设计基准：圆点约 2–4 像素，泡泡与小星约 4–7 像素；同时可见约 2–4 枚，上限 5 枚，允许短暂完全没有粒子。峰值不透明度约 15%–30%，每枚持续 2.5–4.5 秒，上浮约 8–18 像素；每隔约 1–2.5 秒尝试生成一枚，达到上限就跳过，不积攒补播。围绕角色轮廓外沿少量分布，避开脸部、眼睛、呆毛和围裙图案；默认视觉强度为“轻”，远看不应像桌面通知或发光边框。

### 情绪粒子：小而明确的回应

统一采用圆润的自绘符号，细灰蓝轮廓配柔和填色，不直接依赖系统彩色 emoji，以免不同电脑的样式突变。符号放在头部斜上方或发梢外侧，与脸和呆毛留出空隙；主符号高度约为角色身高的 5%–7%（300 像素时约 15–21 像素），辅助符号更小。情绪符号可以比待机微粒稍清楚，但不能盖住表情或占据大块桌面。

| 效果 | 视觉与运动描述 | 候选触发场景与初始节奏 |
| --- | --- | --- |
| 问号 `?` | 一枚浅灰蓝或淡蓝紫的圆润问号，在头旁轻轻浮现、微歪约 5–8°，短暂停留后淡出；可配一个极小圆点，不反复弹跳 | 后续疑惑/歪头动作，或偶发点击回应；约 1.2–1.8 秒。不因鼠标普通经过或静止就不断冒问号 |
| 爱心 | 小型柔和淡粉爱心，在角色周围随机位置出生，从下往上连续飘动并逐渐透明消失；避开表情和关键轮廓 | 下一步优先实现：持续有效摸头期间间隔少量生成，停止后停止新生成，存量自然淡出；限制同时存活数量，替代旧版每轮仅一组方案 |
| 感叹号 `!` | 一枚奶油浅金或淡杏色的圆润感叹号，快速但柔和地从约 85% 放大到正常大小，最多一次很小的回弹，然后消失 | 后续唤醒、突然被拎起或明确的惊讶回应；约 0.6–1 秒。不得绑到普通转头或每次鼠标移动 |

情绪效果只跟随已经发生的交互/动作事件，不独立随机刷屏，也不据此推断用户的真实情绪。建议同一时间只显示一组情绪符号：新的高优先级交互可替换旧组，重复事件忽略，不排队补播；初始优先级可为惊讶感叹号高于摸头爱心、高于疑惑问号。不同情绪组之间建议保留约 2–3 秒真实时间冷却。情绪效果出现时，暂停生成待机微粒，让已有微粒自然消散，避免争抢注意力。打开菜单/设置面板时暂停新效果，关闭后不补发；拖动时暂停待机微粒，已有符号跟随角色移动或柔和消退，不能遗落在桌面上。

### 实现约定与后续安排

- 粒子使用独立绘制层与独立透明度，角色仍保持单源完整绘画；粒子自身淡入淡出不等于允许角色姿势整图叠化。不改现有发色、体型、头发留白、呆毛方向、转头/摸头动作幅度和眨眼遮罩。
- 效果位置采用角色局部锚点，随尺寸、位置及拎起位移同步；情绪符号在前景、待机微粒优先在轮廓外侧或后景。预留绘制边距，避免窗口/屏幕边缘裁切；粒子区域必须鼠标穿透，不扩大原拖动和摸头命中区域，也不能抢焦点。
- 建议提供“待机微粒”和“情绪效果”两个独立开关，保留用户设置。沿用统一调速约定：待机微粒、问号、爱心、感叹号各有独立 0.25–5 倍速度，放在原“调节动画速度…”面板，默认 1 倍、支持数值输入和保存；只改变位移/缩放/淡出的动画进度，触发间隔、冷却与数量上限仍使用真实时间。调慢后也不能突破粒子上限。
- 实现时可新增 `src/PetParticles.cs` 管理生成、生命周期和绘制，在 `src/PetAnimation.cs` 接入角色锚点与已确认触发的动作事件，在 `src/PetApp.cs`、`src/AnimationRates.cs` 接入开关、独立倍率和持久化；若新增源码或图形资源，更新 `build.ps1`。简单符号优先使用 WPF 缓存矢量路径；独立效果不应要求重绘角色图集或重算角色运动场，只有角色素材本身改变时才走原素材流程。
- 控制同时存活数量，复用粒子对象和绘制资源，沿用现有渲染节拍，避免每枚粒子单独建窗口/计时器或每帧加载图片。后台、最小尺寸与慢速播放下仍以低占用、少干扰为目标。
- 最新顺序：先实现持续摸头保持及随机位置上升淡出的摸头爱心，再考虑待机微粒与开关，之后接问号和感叹号。后续体验重点是“不抢眼、符号易懂、无遮挡、不截断、不影响鼠标操作”；功能检查与用户观感分别记录。本次仅记录方案，不生成素材、不修改代码、不构建或部署，也不改变当前摸头待验收状态。

## 开发记录

### 2026-09-18：消除重叠并补足过渡

- 单源姿势渲染替代整图叠化；新增八方向半程和闭眼素材，共 50 帧。
- 496 组原生 WPF 渲染检查通过；包含单源颜色/透明度约束、眼外像素不变、动作衔接、倍率、五秒回正及缩放边界。
- 用户实机反馈：顺滑；但旧正视与新姿势色彩不一致。

### 2026-09-18：统一正视并发布 v0.1

- 正视帧 0 和副本 8 改用 `v6/bridges/17.png` 第一格；帧 2 的闭眼素材取新闭眼九宫格中心。保持已确认的其他方向与动作倍率。
- 修改 `.build/prepare-v6.cjs`，确保再次生成图集时正视替换可重复，不靠手工覆盖。
- 重新导出原生帧、计算 141 对运动场并运行回归检查：496 组原生渲染通过，单源绘制、眼外像素不变、倍率和交互状态检查均通过。已检查切换点对照图，构建并启动新版；旧可执行文件备份至 `_archive/before-neutral-时间戳.exe`。
- 新增本文件和 `AGENTS.md`，要求后续每次开发维护本文档。
- 按用户指定将当前完整版本统一标记为 v0.1.0；程序集、诊断记录、图集元数据、README 和可执行文件使用同一版本号。同步整理到私有 GitHub 仓库并建立 `v0.1.0` 标签。

### 2026-09-18：记录下一步生动化方案

- 用户目标：先把下一步开发想法写入文档，为让角色更生动选择功能。
- 变更：仅更新 `开发文档.md`，增加自主小动作首选方案、后续候选、预计文件和验收要求；未修改代码、素材、程序或用户设置。
- 文档核对：已查阅当前交互、动作状态、动画倍率代码及 v6 图集元数据；确认方案与现有接口的接入位置。本次为文档变更，未运行构建或动画自动测试。
- 人工反馈：用户提出“更生动”的方向，尚未选择具体方案；没有新增功能的体验验收。
- 遗留：待选择下一项功能后实施；提案中的时间参数需实机调整。

### 2026-09-18：实现自主左右张望

- 用户目标：先做左右张望，让安静待机更生动。
- 实现与文件：`src/PetAnimation.cs` 新增可注入随机源的 `IdleLook` 调度器，接入原有转头追随和动作中断；`src/PetApp.cs` 增加菜单、交互暂停及设置读写；`.build/KeyframeChecks.cs` 增加自动行为检查；同步本文档和 `使用说明.md`。构建流程与素材未改动。
- 自动验证：候选 EXE 编译成功；验证 0.25、1、5 倍下多轮张望到达左右完整姿势、交替起始方向、回正后完成、冷却、频率范围、鼠标/点击/菜单/禁用/拎起中断时的连续性，以及新增配置默认值和读写往返。原有 496 组原生 WPF 渲染及倍率、单源绘制、眼外像素、起落和缩放边界检查通过。现存 `FormattedText` 过时警告仍在。
- 部署验证：正常关闭旧进程后替换，备份位于 `_archive/before-idle-look-20260918-195908`；新版启动且窗口进程响应正常，设置文件已写入新默认项，逐行核对原设置保留。未提交或上传，当前工作目录不是 Git 仓库。
- 人工反馈：尚未收到用户对新增张望的实机反馈；自动渲染检查和进程存活不等于桌面视觉验收。
- 遗留：请实机确认动作节奏、后台顺滑程度及张望中打断的主观观感；后续按反馈调整，不提前认定已验收。

后续记录格式：日期、用户问题/目标、实现与文件、自动验证、人工反馈、遗留问题。不要把待验证事项写为已解决。

### 2026-09-18：独立张望倍率、摸头回应与统一调速约定

- 用户要求：张望调速放进原有界面，新增摸头动画；以后所有动作独立调速并统一在该面板。长期规则已写入“代码入口”下的专节。
- 实现：`src/AnimationRates.cs` 追加 IdleLook、HeadPet，动作名称集中维护；`src/PetApp.cs` 原速度面板动态显示九项，重置覆盖全部动作，新增倍率按旧机制保存，旧配置缺项默认 1 倍。张望频率与速度继续独立。
- 摸头：不按鼠标，在头顶来回轻扫约半秒以上触发。识别使用角色标准坐标中的椭圆区域（中心 170,102，半轴 68,38），随显示大小换算；1.8 秒内累计横向移动至少 80 单位、两次有效换向，过滤小于 12 单位抖动。静止、单次掠过、身体区域、按键拖动和菜单中不触发。
- 动画：闭眼、轻低头、睁眼恢复；复用 v6 正视和下方半程实体姿势及原眼部遮罩，1 倍约 2.4 秒，完成后冷却 2 秒。摸头期间暂停自主张望，点击/菜单/拖动可中断，拎起捕获当前姿势。没有新增素材或更改运动场，无须重新导出素材。
- 涉及文件：上述两文件、`src/PetAnimation.cs`、`.build/KeyframeChecks.cs`、本文档和 `使用说明.md`。
- 自动验证：新增手势误触过滤、0.25/1/5 倍回应时长与闭眼低头、冷却、中断连续性、两项独立倍率保存检查通过；原有张望回归及 496 组 WPF 渲染、眼外像素、单源、倍率和交互检查通过。候选 EXE 编译成功，现存 FormattedText 过时警告不影响构建。
- 画面检查：已查看离屏摸头序列，闭眼、低头及恢复阶段可见；测试图位于 demo/animation-qa/head-pet-sequence.png，仅为本地 QA，不应纳入上传。该检查不代表用户已验收。
- 部署：旧版正常关闭后替换并启动新版，备份目录 `_archive/before-head-pet-20260918-200849`，原设置文件保留。未提交上传。
- 实机边界：已看到新版桌宠窗口；准备打开速度面板时用户按 Escape 停止 Computer Use，已停止所有 UI 操作。实际面板操作、真实鼠标摸头触发及用户观感尚未完成实机验收。

### 2026-09-19：更换桌面快捷方式图标

- 用户要求：将本次提供的头像 PNG 转成 ICO，并用于桌面“大肥鱼”快捷方式。
- 涉及文件：新增 assets/desktop-avatar.ico，包含 16、24、32、48、64、128、256 像素七档图标，保留原图完整构图和颜色；修改桌面“大肥鱼.lnk”的 IconLocation；同步本文档。
- 原快捷方式与修改前文档备份：C:\chatgpt\大肥鱼桌宠\_archive\desktop-icon-20260919-072702。
- 自动验证：ICO 可由 Windows System.Drawing.Icon 解码；快捷方式重新读取后确认图标路径正确、启动目标保持原值；已发送系统图标刷新通知。
- 用户体验反馈：尚待用户查看桌面显示效果，未将自动检查视为用户验收。
- 未解决问题：本次无新增已知问题；仅转换快捷方式图标，不涉及动画素材、原生帧、运动场、程序构建或用户设置。顶部旧摸头任务仍待后续单独处理。

### 2026-09-19：快捷方式头像透明外沿与放大

- 用户反馈：上一版图标有外围白边，要求改透明并让彩色区域尽可能占满图标。
- 制作：使用内置 imagegen 编辑原头像，提示词要求仅移除蓝色圆角矩形外白边、保留内部白色细节与角色、紧裁切并输出透明 PNG；第二轮要求清理外围白色碎边。新增 assets/desktop-avatar-transparent.png、assets/desktop-avatar-transparent.ico 及 .build/convert-desktop-icon.ps1。
- ICO 包含 16、24、32、48、64、128、256 七档；桌面“大肥鱼.lnk”已切换到新 ICO 路径并通知系统刷新。旧 ICO 保留；快捷方式及文档备份位于 _archive/transparent-icon-20260919-073147。
- 验证：PNG 左上角 alpha 为 0，ICO 可解码，快捷方式读回路径正确且启动目标未变。已查看生成预览，大面积白色外框已去除，彩色区域接近画布边缘；生成图高分辨率边缘仍可能有细小残留，未做桌面视觉验收。
- 用户体验：等待用户查看实际图标；自动验证不代表验收。仅更改快捷方式图标，不涉及动画、运动场、程序或设置。

### 2026-09-19：专用开心轻蹭摸头动画

- 用户要求：先阅读文档并提出设计；用户确认“可以”后制作并接入。确认方案为开心挤眼 → 朝鼠标轻蹭一下 → 更小幅补蹭 → 回正后稍作停留再睁眼，全程约 2.6 秒（1 倍）。
- 素材：内置 imagegen 以 bridges/17.png 左侧当前正视角色为参考，生成 head-pet-sheet.png 九个专用完整姿势，追加为帧 50–58。完整提示词存于 assets/animations/v6/head-pet-generation.md。制作目标保留外观与不对称细节，未通过镜像制作左右方向，未添加爱心等装饰；实际外观尚未检查。
- 动作：src/PetAnimation.cs 将旧低头闭眼逻辑替换为专用帧序列；根据触发时头部标准坐标 x<158、x>182 或中间区域锁定左、右、中央方向。第一下完整轻蹭，第二下使用半程至完整姿势间 55% 的幅度，2.08–2.20 秒短暂保持正面开心表情后恢复。
- 衔接：从触发前的跟随姿势进入；点击/菜单打断时用当前姿势向正视作 0.28 秒恢复（受 HeadPet 倍率控制），拖动继续捕获当前姿势。动作结束后从正视恢复正常跟随。摸头专用绘画跳过普通闭眼素材，期间暂停普通眨眼；原普通眨眼仍只修改眼睛区域。渲染继续使用单源完整绘画与相邻姿势运动场，无整图透明叠化。
- 设置：沿用现有手势识别、真实时间冷却和 HeadPet 独立 0.25–5 倍，沿用原速度面板、即时变速与保存逻辑，没有更改配置结构。
- 制作文件：src/PetAnimation.cs、.build/prepare-v6.cjs、.build/analyze-v6-motion.py、.build/deploy-head-pet.ps1；新增上述素材与生成记录，重新生成 v6/unified-sheet.png、atlas.json、motion-fields.gz；同步使用说明.md 和本文档。
- 制作结果（不是验证）：图集整理为 59 帧；仅运行 check-native-animation.ps1 export 导出原生像素，没有执行检查分支；使用内置 Python 的 NumPy 和项目现有 OpenCV 完成 174 对运动场计算。依赖目录在沙盒内不可访问，已通过受控提权执行该计算。候选程序编译完成。
- 部署：正常关闭项目桌宠，备份并替换 大肥鱼桌宠.exe，已发送启动请求。备份目录 _archive/before-nuzzle-20260919-073752，包含原程序、源码、原 v6 素材、原制作脚本、原文档和退出前后设置副本。保留实际 data/settings.txt，未提交或上传。
- 检查与用户反馈：按用户要求未检查，等待用户自行体验反馈。没有执行自动回归、渲染对照、生成图审查、UI 自动操作或实机验收；编译、原生帧导出与运动场计算仅作为制作步骤，不代表视觉通过。
- 未解决问题：旧版蓝色残留是否消失、动作幅度/衔接/画风是否符合预期均待用户体验；.build/KeyframeChecks.cs 中旧 HeadPetChecks 仍断言旧版 2.4 秒低头闭眼，后续如用户要求恢复检查，需先更新为新版帧序列预期，本次未运行或修改该回归。

### 2026-09-19：固定微抬左右轻蹭与跟随恢复调整

- 用户体验反馈：上一版“摸完鼠标放到两边会抽搐”；用户要求蹭手改为固定往上微抬后左右轻微蹭动，不再检测鼠标位置来改变蹭头朝向。此反馈未因旧制作步骤完成而视为已解决。
- 动作与素材：使用内置 imagegen 生成 assets/animations/v6/head-pet-fixed-sheet.png（完整提示词在 head-pet-fixed-generation.md）。沿用帧 50–58 索引，将左右轻蹭改为保持轻微抬头的实体完整与半程姿势；保留旧图作历史参考。每次固定走正面开心 → 微抬 → 左半程/完整 → 微抬中央 → 右半程/完整 → 微抬中央 → 回正睁眼，约 2.6 秒，保留独立 HeadPet 倍率。
- 跟随衔接：src/PetAnimation.cs 移除 petDirection 和根据触发位置选择方向的分支；鼠标坐标仍用于识别手势和正常跟随。摸头、打断恢复及结束短暂停稳期间不选择鼠标跟随目标，结束时清除旧目标并停在正视 0.18 秒，再用转头倍率推进 0.4 秒目标渐入，沿用原阻尼追随。这样避免积存的侧向目标在结束时立即接管；是否为用户所见抽搐的完整成因没有实机确认。
- 涉及文件：上述源码与两份新素材记录，.build/prepare-v6.cjs、.build/analyze-v6-motion.py、.build/deploy-head-pet.ps1、v6/unified-sheet.png、atlas.json、motion-fields.gz，使用说明.md 和本文档。
- 制作结果：图集仍为 59 帧；仅运行原生 export 分支导出像素，重算 176 对运动场（新增中央微抬与左右半程连接），候选程序编译完成。正常关闭原程序、备份并替换后发送启动请求；未修改设置结构或实际设置文件。备份位于 _archive/before-fixed-nuzzle-20260919-074900。
- 验证边界：按用户要求未检查，等待用户自行体验反馈。没有运行自动回归、图片审查、动画对照或实机验收；编译和位移场计算不等于验证通过。抽搐是否消失、微抬与左右幅度是否合适、旧蓝色残留是否消失均待用户反馈。

### 2026-09-19：保持抬头闭眼、连续水平转头蹭动

- 用户实机反馈：固定轻蹭版仍类似点头；要求检测到摸头后“抬头鼻炎然后左右来回轻微转头模拟蹭的感觉”。本轮按上下文将“鼻炎”理解为“闭眼”，保留既有 > < 开心闭眼风格。
- 制作：内置 imagegen 新建 head-pet-yaw-strip.png 水平转头五姿势条，并修订右侧不对称细节。实际只选中间三张小角度实体姿势（约左 4 度、正面、右 4 度的生成目标），追加为 59–61；不使用两端大角度。提示词和选帧说明见 assets/animations/v6/head-pet-yaw-generation.md。实际角度与外观未测量或验收。
- 时间设计：1 倍共 3 秒；0–0.35 秒进入开心闭眼，0.35–0.65 秒只抬头一次，0.65–2.35 秒保持抬头角度执行连续左右转头，2.35 秒后才恢复头部并睁眼。蹭动用单个正弦周期与首尾包络，不在中心/中间姿势重新缓入缓出；抬头和恢复各自使用一次整体缓动，不在半程停顿。
- 保留：手势触发、拖动/菜单打断、独立 HeadPet 倍率、真实时间冷却、结束正视停稳与普通跟随渐入，均不根据鼠标位置选择蹭动方向。继续单源绘画采样与几何补间，不使用整图透明叠化或旧闭眼遮罩覆盖开心脸。
- 文件：src/PetAnimation.cs、.build/prepare-v6.cjs、.build/analyze-v6-motion.py、.build/deploy-head-pet.ps1、新增素材与生成记录、重建 v6 图集/坐标/运动场、使用说明.md 和本文档。
- 制作结果：62 帧图集，原生 export 导出完成，182 对运动场计算完成，候选程序编译完成。正常关闭旧程序并备份、替换，已发送启动请求；实际设置文件保留。备份目录 _archive/before-yaw-nuzzle-20260919-075654。
- 验证结果与遗留：按用户要求未检查，等待用户自行体验反馈。没有运行自动回归、对照渲染或实机验收；上述制作步骤不表示点头感、抽搐或蓝色残留已消失。等待用户评价左右转头蹭动是否符合预期。

### 2026-09-19：记录周身粒子与情绪表达设计

- 用户要求：根据桌宠风格，将待机时小而不抢眼的周身粒子，以及问号、爱心、感叹号情绪粒子的开发描述先写入文档。
- 涉及文件：仅开发文档.md，新增“周身微粒与情绪表达效果设计”章节；记录配色、形态、大小密度、运动节奏、候选触发、去重冷却、遮挡/交互边界、独立调速和建议实现顺序。所有参数与触发场景均标为待实现提案。
- 文档核对：参考当前角色画风、既有调速约定以及 PetSpriteView/窗口坐标与绘制入口，完成新增章节读回；未运行程序构建或动画测试。
- 用户体验反馈：用户提出效果类别与轻微、不抢眼的要求；尚无粒子视觉样稿或实机验收。
- 未解决问题：粒子具体视觉强度与情绪触发场景待后续制作和体验确认；原摸头效果反馈仍保留。修改前文档已备份至 _archive 下的 particle-design-时间戳目录。

### 2026-09-19：合入 GitHub 平滑缩放修复并重做参考图摸头

- 用户要求：将 GitHub 仓库他人提交的缩放流畅度修复合入本地；随后补充摸头重做要求，反馈旧动作触发后角色变宽，要求按参考图双手胸前、弯眼微笑、保持低头左右轻扭撒娇。本轮一并完成源码接入与部署，不覆盖此前粒子设计文档。
- 上游来源：LdliMiku/fat-fish-desktop-pet 提交 5b688c5a80c3f999a61132146980bb8eebc4f492（Fix desktop pet resize flicker with stable surface and smooth scaling），主分支已通过 0ac9848e2546185567cc9d05cbfc8cea79c42198 合入。来源链接：https://github.com/LdliMiku/fat-fish-desktop-pet/commit/5b688c5a80c3f999a61132146980bb8eebc4f492 。只移植所需差异，未整文件覆盖本地新增交互，未写远程仓库或改动独立的 v0.9 目录。
- 缩放实现：新增 src/PetSizeMotion.cs；src/PetApp.cs 改为 696×736 DIP 固定透明窗口、340×360 角色布局和脚底中心矩阵缩放，滚轮/滑块/输入平滑逼近目标值；点击/拖动停止在当前可见尺寸。边界、设置窗口定位、找回及保存仍按可见角色外框计算，沿用 layout=3 坐标。保留本地张望/摸头交互及独立倍率；缩放过程中清空摸头采样，防止尺寸变化使局部鼠标坐标变化而误触发。build.ps1 纳入新源码。
- 缩放测试：移入上游 tests/ResizeWindowChecks.cs、tests/check-resize.ps1，并将上游 SizeChecks 提取成独立 tests/ResizeMotionChecks.cs；运行目录隔离到 .build/resize-check-runtime，finally 关闭前禁用设置保存。最终合并候选 EXE 编译成功；30/60/144 Hz 收敛、反向输入、延迟帧、脚底锚点与正负屏幕边界检查通过；240 次隐藏 WPF 窗口缩放帧中 WM_SIZE 为 0、角色布局不变、脚底/点击反馈锚点固定、目标尺寸收敛。此专项检查不触发或验收摸头，不能替代实机流畅度、高 DPI 和多屏体验。
- 摸头素材：新增 assets/animations/v6/head-pet-chest-reference.png、head-pet-chest-sheet.png 和 head-pet-chest-generation.md（内置 imagegen 提示词与修订记录）。原画保留正常站姿体型，以用户图为手势表情依据，替换旧 > < 与抬头表现；九宫格统一使用正常站姿高度与原正视宽度系数，去掉旧 yaw 独立素材按自身宽高重新标定的横向倍率。旧变宽完整成因未实机诊断，不能据此宣称已消失。
- 摸头动作：src/PetAnimation.cs 在 0–0.8 秒回正、收手、闭眼微笑并轻低头；0.8–2.6 秒双手胸前、低头状态下沿连续正弦曲线小幅左右扭头，使用完整左右姿势幅度的 70%；2.6–3.2 秒放手并恢复。保留 HeadPet 0.25–5 倍、真实时间触发/冷却、中断和恢复跟随逻辑。普通眨眼不覆盖新表情，单源绘画规则保留。
- 制作文件：src/PetAnimation.cs、src/PetApp.cs、src/PetSizeMotion.cs、build.ps1、tests 下三项缩放专项文件、.build/prepare-v6.cjs、.build/analyze-v6-motion.py、.build/deploy-smooth-resize-chest.ps1、新摸头素材/记录、v6 图集/坐标/运动场、使用说明.md 和本文档。
- 素材制作结果（不是视觉验证）：59 帧图集，原生 export 导出完成，157 对双向运动场计算完成。没有运行旧 HeadPetChecks 或新摸头回归、渲染对照、桌面手势验收。
- 部署与备份：正常关闭旧程序，备份并替换 大肥鱼桌宠.exe，发送启动请求。缩放前源码/原程序及退出前后设置备份：_archive/before-smooth-resize-20260919-130523；摸头重做前素材/源码备份：_archive/before-chest-pet-20260919-130734。保留原 data/settings.txt；未提交上传。
- 用户反馈与遗留：按用户要求未做摸头动画检查，等待用户自行体验反馈。新双手胸前姿态、弯眼表情、低头扭动、是否仍变宽/抽搐均待用户判断。上游对其预览版的既有验收不等于用户验收本地合并版；固定较大透明表面的合成开销、高 DPI 与多屏行为仍待实机观察。
- 部署读回：新版进程存在且响应。设置中的大小、置顶、张望选项和全部动画倍率保持原值；可见位置 left/top 分别有约 0.068/0.137 DIP 的舍入差异，记录为窗口坐标换算后的亚像素偏差，没有据此宣称位置逐字节一致。此读回不是摸头或缩放实机视觉验收。

## 2026-09-19 原肤色与原下方半程动作重做

- 用户反馈：上一版脸部颜色偏离原肤色，转头幅度过大，双手需自然收在胸前。该反馈覆盖此前素材的完成记录，不代表此前版本已验收。
- 素材制作：`.build/prepare-pet-pose-reference.cjs` 从原始帧 0、39、40、41 按运行比例排版为 `assets/animations/v6/head-pet-original-pose-reference.png`，用内置 imagegen 局部编辑生成 `head-pet-original-tone-sheet.png`；完整提示保存在同目录 `head-pet-original-tone-generation.md`。提示要求保留原肤色，仅增淡腮红，使用弯眼微笑和自然胸前双手，不重新设计转头轮廓。
- 动作接入：`src/PetAnimation.cs` 的左右摆动改为中心 55 与左 54／右 56 之间最高 0.60 权重，取消向 53／57 大幅端点延伸；保持低头，不根据鼠标方向改变蹭头朝向。收手与放手仍有独立中间绘画。保留独立 HeadPet 速度和缩放流畅度修复。
- 尺度：`.build/prepare-v6.cjs` 改用新素材，整组统一采用第一格的标定高度。底图已包含原宽度修正，新素材不重复乘该倍率，不给左右姿势分别归一化宽高。
- 制作结果：图集 59 帧、原生帧导出、157 对运动场计算及候选程序编译已完成。这些是制作步骤，不是动画验证结果。原生导出只有旧 FormattedText API 编译警告。
- 部署：正常关闭旧程序，备份至 `_archive/before-skin-pose-20260919-134400`，替换根目录程序并发送启动请求；部署脚本 `.build/deploy-original-tone-pet.ps1` 不覆盖用户设置文件。未修改独立 v0.9 仓库。
- 验证与未解决项：按用户此前要求未运行动画检查、图像对照或实机验收。无法据此确认生成图完全保持原肤色，也不宣称变宽、脸部残留或手势自然度已通过验证；等待用户自行体验反馈。本轮未修改缩放实现，未重复缩放专项检查。

## 2026-09-19 轻蹭后发与分片线固定、结束直接放手

- 用户最终澄清：摸头期间双手仍在胸前，结束时直接放下；特别关注后发分片轮廓线、发束分界线的跳变。
- `src/PetMotionWarp.cs`：仅对 54／55／56 轻蹭组合固定 55 为唯一绘画源，不在权重超过一半时改用另一张绘画。运动场只作用于头脸前部椭圆区域，边缘平滑衰减；外部后发、发束分片线、手与身体逐像素复制中心源，避免整片后发随光流变形。没有整图透明叠化。该区域是固定空间范围，并非自动头发语义分割，边缘自然度仍待用户反馈。
- `src/PetAnimation.cs`：保留原收手进入阶段和轻蹭幅度，结束时直接使用 0 自然垂手姿势，不再经过 58 或放手插值。直接回位同时恢复头部与原姿态；不能将轻蹭阶段后发固定等同于进出动作全程无跳变。
- 未更改素材，沿用原生帧与已有运动场，无需重复导出。候选编译完成；正常关闭旧程序并备份至 `_archive/before-fixed-hair-20260919` 后替换根目录可执行文件，已发送启动请求。用户设置保留，缩放和独立速度未更改。
- 按用户此前要求未做自动动画检查、截图或实机验收。生成与编译不代表视觉通过；分片线稳定性与动作衔接等待用户自行体验。

## 2026-09-19 取消摸头左右转头

- 按最新要求修改 `src/PetAnimation.cs`：保留表情和胸前收手进入过程，0.80–2.60 秒固定使用 55 中心绘画，删除左右摆动及 54／56 混合；结束继续直接回到 0 放下手。时长随原 HeadPet 独立倍率调整。
- 沿用当前素材（含轻低头姿态），未修改图集或运动场，不需重新导出；本次取消的是动作中的左右转动，进入时仍有原有姿态过渡。
- 编译完成，正常关闭旧程序并备份到 `_archive/before-no-head-turn-20260919-140727`，替换本地程序并发送启动请求，未覆盖设置。部署脚本为 `.build/deploy-no-head-turn-pet.ps1`。
- 按此前约定未做动画检查或实机验收，等待用户自行体验；编译完成不代表视觉验收。

## 2026-09-19 开发进度整理与下一步目标

- 用户要求将当前开发进度写入文档，并明确下一步增加摸头爱心：随机位置生成、从下往上升、逐渐消失；补充持续有效摸头时不得退出摸头动作。
- 仅修改 `开发文档.md`：重整顶部交接，区分已接入的无转头摸头版本与尚未实现的持续保持／粒子；同步当前状态、爱心方案和开发优先级，保留历史记录。
- 本次以当前 `HeadPetPose` 代码核对固定时长现状；文档编辑未运行程序测试或视觉验收，未构建、部署或实现新功能。下一步按顶部目标开展制作，效果参数与用户体验仍待后续确认。

## 2026-09-19 优先接入摸头爱心粒子

- 用户本轮明确先加爱心效果；持续有效摸头不退出的状态机尚未制作，仍作为下一步。
- 新增 `src/PetParticles.cs`：缓存 WPF 矢量爱心、柔和淡粉填色与灰蓝描边；在角色局部坐标两侧外沿随机出生，位置只随机一次。约每 0.32–0.52 秒生成一枚，最多 6 枚；宽约 10–14 个场景单位，1 倍寿命约 1.5–2.1 秒，上升约 65–90 个场景单位并略向外漂移，短暂淡入后逐渐淡出。具体观感参数尚待用户体验。
- `src/PetAnimation.cs`：沿用现有渲染节拍，在摸头进度 0.38–2.60 秒生成粒子；放手、交互阻塞或中断后不再新增，已有粒子继续淡出。粒子在角色绘画之外单独绘制，沿用角色局部缩放与位置，并跟随拎起的垂直位移；绘制坐标保持在现有 340×360 场景内，不增加窗口或交互控件，不改角色素材与头发绘画。停顿后不补发积攒粒子。
- `src/AnimationRates.cs`：末尾追加 Hearts／“摸头爱心”，原速度面板自动显示，0.25–5 倍、默认 1 倍、保存键 speed_Hearts；旧设置缺项仍为 1，已有速度不变。倍率控制上升与淡出的生命周期，出生间隔按真实时间，慢速下数量仍受上限约束。
- `build.ps1` 与 `.build/check-native-animation.ps1` 增加新源码依赖；本轮未运行后者。没有修改角色素材，无需重新导出图集或运动场。
- 制作结果：候选编译完成，正常关闭旧程序并备份到 `_archive/before-heart-particles-20260919-183227`，替换本地程序并发送启动请求；用户设置保留。部署脚本为 `.build/deploy-heart-particles.ps1`。
- 验证边界：按用户之前要求未运行自动动画检查、截图或实机验收；编译不等于视觉验收。爱心位置是否遮挡、缩放时边缘观感、速度设置实机操作及整体密度待用户体验。本次未实现待机微粒、问号、感叹号或粒子开关。

## 2026-09-19 增强摸头爱心醒目程度

- 用户实机反馈：初版爱心太小、不醒目，需要醒目地出现在角色周围。
- 修改 `src/PetParticles.cs`：主体宽度约 26–36 场景单位（原 10–14），鲜粉填色、深粉边线与浅白外缘；7% 生命周期内淡入，至 55% 保持不透明，随后平滑淡出。左右交替出生、每枚位置随机，纵向出生范围扩大为 128–270，向上移动 60–80；生命周期约 1.8–2.3 秒，出生间隔约 0.25–0.38 秒，上限仍为 6。独立速度与角色动画不变。
- 已编译并正常关闭、备份和替换本地程序，发送启动请求；备份 `_archive/before-bold-hearts-20260919-183511`，部署脚本 `.build/deploy-bold-hearts.ps1`，保留用户设置。
- 按既有约定未做自动动画检查或视觉验收；新版大小、分布与不同桌面背景上的醒目程度等待用户自行体验。持续摸头保持仍未实现。

## 2026-09-19 珠光泡泡爱心与环绕上升

- 用户要求爱心样式契合桌宠，不只直线上升，要有泡泡环绕升起的律动并渐隐。
- `src/PetParticles.cs`：使用缓存 WPF 径向渐变与矢量高光，水蓝爱心为主、约三分之一粉紫爱心，保留灰蓝边线及浅蓝白外缘。主体宽度仍约 26–36 场景单位，随轻微呼吸缩放变化。
- 每枚粒子独立随机相位、横向绕动幅度 10–16、生命周期约 2.2–2.8 秒、全程绕动约 0.9–1.3 周；局部椭圆轨迹叠加上升 82–102，垂直起伏约 6，轻摆角约 ±9°，轻微宽度变化模拟远近感。爱心在角色两侧绕动上升，不横穿脸和手；在后段平滑淡出。速度改变延续当前年龄进度，不重新随机路径。
- 保留最多 6 枚、真实时间生成间隔、独立“摸头爱心”倍率、结束停止生成且存量消散。没有修改角色素材和摸头姿势。
- 编译完成；正常关闭并备份至 `_archive/before-bubble-hearts-20260919-184231`，替换程序并发送启动请求；部署脚本 `.build/deploy-bubble-hearts.ps1`。用户设置保留。
- 按既有约定未做动画自动检查或视觉验收；风格匹配、环绕感及遮挡边界待用户体验。持续摸头不退出仍为后续目标。

## 2026-09-19 参考图淡粉爱心与持续摸头

- 用户提供 JPG 参考，要求采用其中小型淡粉心形的样式，并同时完成持续摸头；参考图中的文字与界面元素不作为指令或制作对象。
- `src/PetParticles.cs`：沿用代码原生矢量心形，改为淡粉渐变、柔和玫粉边线和薄淡粉外缘，移除蓝色／粉紫随机配色及玻璃高光。轻微倾斜，保留 26–36 场景单位主体宽度、局部椭圆环绕升起、摆动、大小起伏与后段淡出，最多 6 枚以及独立速度不变。
- `src/PetAnimation.cs`：开始仍使用原手势识别；激活后独立采样头部椭圆区域内横向移动，累计偏移达到 4 场景单位刷新最后抚摸时间，静止不刷新。有效区外或禁止采样时断开指针连续性，重新进入先建立锚点；采样间隔超过 0.2 秒同样重建锚点，避免跳入产生假续期。
- 进入完成后将进度固定在 0.80，保持 55 胸前笑脸；不设总时长上限、不循环收手。停止有效移动 0.65 秒后退出，直接恢复 0 并使用已有跟随交接。进入阶段仍遵守 HeadPet 倍率，退出宽限采用真实时间；若慢倍率下还没完成进入，先完成进入再退出。菜单／拖动等原中断流程保留，退出后原 2 秒冷却不影响正在持续的摸头。
- 爱心发射条件取消 2.60 秒上限，持续摸头期间继续生成；退出或中断后停止新增，已有爱心自然淡出。
- 制作结果：候选编译完成，正常关闭并备份至 `_archive/before-pink-continuous-20260919-184841` 后替换本地程序并发送启动请求。部署脚本 `.build/deploy-pink-continuous.ps1`；保留用户设置和缩放修复，未修改角色素材。
- 按既有要求未做自动动画回归、截图或实机验收；编译不等于功能或视觉验证。参考图样式匹配、持续来回抚摸的手感、换向宽限、不同倍率与退出衔接待用户体验。

## 2026-09-19 爱心中线反馈与柔化

- 用户报告爱心中间一直有竖线、样式不柔和。源码排查发现凹口具有反向竖直切线形成的尖点，叠加默认 miter 描边可能形成内向尖刺；这是代码层面的原因判断，未进行截图或实机确认。
- 修改 `src/PetParticles.cs`：改为带圆滑凹口、圆化底尖的单一闭合轮廓，主边线和外缘均设置 PenLineJoin.Round；主线宽由 0.10 降为 0.065，外缘由 0.18 降为 0.14，并减淡颜色、柔化粉色渐变。没有新增内部线条或分片填色。
- 编译完成，正常关闭并备份到 `_archive/before-soft-heart-20260919-185126`，替换本地程序并发送启动请求；脚本 `.build/deploy-soft-heart.ps1`。保留设置、粒子运动和持续摸头。
- 按既有约定未做自动动画检查或视觉验收；竖线是否消失、柔和程度是否合适待用户体验，不能将编译成功写作视觉修复已验证。

## 2026-09-19 生成爱心图片并套用现有粒子逻辑

- 用户指出函数绘制的爱心生硬，要求生成简单图像再复用粒子生成逻辑。
- 内置 imagegen 生成柔和粉色手绘单心、透明背景，保留原图 `assets/particles/pink-heart-source.png`，完整提示 `assets/particles/pink-heart-generation.md`。`.build/prepare-heart.cjs` 仅裁去透明边距、等比缩放与添加透明留边，输出 128×128 的 `assets/particles/pink-heart.png`，不程序化重画心形。
- `src/PetParticles.cs` 移除矢量 Geometry 与描边／渐变绘制，改为首次加载内嵌资源 HeartParticle、冻结缓存、每枚调用 DrawImage；继续使用相同随机位置、生命周期、环绕轨迹、摇摆与透明度逻辑，不逐帧加载磁盘文件。持续摸头和独立 Hearts 倍率不变。
- `build.ps1` 及原生导出／检查入口 `.build/check-native-animation.ps1` 增加图片资源；本轮未运行检查入口。角色原生帧及运动场未修改，不需重新生成。
- 制作结果：素材处理和候选编译完成，正常关闭旧程序并备份到 `_archive/before-image-heart-20260919-185425`，替换根目录程序并发送启动请求。脚本 `.build/deploy-image-heart.ps1`，保留用户设置。
- 按用户既有要求未做生成图审查、自动动画检查或实机视觉验收；贴图风格、缩小显示及实际柔和程度待用户体验。

## 2026-09-19 摸头退出柔和过渡

- 用户反馈直接退出摸头太生硬，要求增加过渡；最新要求覆盖此前直接放手决定。
- `src/PetAnimation.cs` 新增 HeadPetExitPose 与自然退出标记：持续摸头停止超过 0.65 秒真实时间后，进入 0.65 秒（1 倍）退出动画，依次 55→52（0.20 秒）、52→51（0.22 秒）、51→0（0.23 秒），利用已有实体中间绘画与双向运动场，缓动恢复表情并放下双手。退出速度服从 HeadPet 倍率，结束才恢复原正视停稳与跟随交接。
- 菜单等中断保留从当前姿态开始的原 0.28 秒恢复，不强行跳到完整胸前姿势；拖动沿用现有拎起逻辑。退出期间不生成新爱心，已有爱心沿原轨迹淡出。
- 未改角色素材，复用已有帧和运动场，不重新生成素材或使用整图透明叠化。编译完成，正常关闭旧版并备份至 `_archive/before-soft-pet-exit-20260919-185754`，替换本地程序并发送启动请求；部署脚本 `.build/deploy-soft-pet-exit.ps1`，设置保留。
- 按既有要求未做自动动画检查或实机视觉验收；手势、表情与头发在退出过程中的连续性仍待用户体验。

## 2026-09-19 上方三方向发梢越界与头顶色差修复

- 用户截图反馈：左上、正上、右上发梢有超出轮廓的残色，头发上部比其他朝向深，切换跳脱。
- 来源排查：`.build/prepare-v6.cjs` 此前三张完整朝向 16–18 继承 v5 图集，34–36 半程与正视来自较新绘画；原图对照可见边缘残色与局部裁切问题。18 还覆盖不同来源的旧头顶补丁，容易增加色调差异。这不代表所有瞬间过渡瑕疵都已定位。
- 内置 imagegen 编辑 `upward-hair-reference.png`（原 0、16、17、18、0），请求保持原视角、角色比例、肤色和服装，仅整理头发边缘、恢复完整发梢并以正视匹配头顶色调；结果 `assets/animations/v6/upward-hair-fixed.png`，完整提示同目录 `upward-hair-generation.md`。
- `.build/prepare-v6.cjs` 用新图第 2–4 格替换 16–18，第 1 格仅统一高度标定，其他帧保留原来源。`src/PetAnimation.cs` 不再为 18 套旧头顶补丁，21／24 保留。更新图集、59 帧原生导出及 157 对运动场，编译候选程序。
- 验证：`.build/check-upward-hair.ps1`／`UpwardHairChecks.cs` 对上方三方向半程到完整姿势执行 39 组原生 WPF 渲染，检查预乘透明像素合法性与画布边缘未裁切，通过；查看 `.build/upward-native-after.png` 的正视、半程、完整姿势静态对照，未见截图中明显淡色残弧，头顶色调更接近。此专项针对普通上方朝向，不是摸头回归，也不代表用户实机验收。现有原生导出有旧 FormattedText API 警告，不阻止编译。
- 部署：正常关闭旧程序，备份到 `_archive/before-upward-hair-20260919-190345`，替换根目录程序并发送启动请求，脚本 `.build/deploy-upward-hair.ps1`。用户设置保留，摸头、贴图爱心及退出过渡未修改。
- 未解决／待确认：生成绘画可能存在细节差异，实际鼠标来回切换的色彩连贯性、发梢边缘和眨眼眼区配准尚待用户体验；没有将静态或数值检查表述为完全消除全部视觉问题。

## 2026-09-19 撤下体型漂移素材，统一上方完整与半程来源

- 用户实机反馈优先于前轮检查：仍有少许溢色、头发色差未解决，且新完整姿势改变形象大小。前次 39 组检查只覆盖透明格式和画布边界，未覆盖体型／发色一致性，不能据此声称已修复。
- 排查发现新生成五联图与旧半程姿势比例不同，用生成图第一格高度统一缩放不能恢复原始身体比例。此次不再整图重生成或按各动作单独硬拉伸。
- `.build/prepare-v6.cjs` 撤销 upward-hair-fixed.png 覆盖，完整 16／17／18 改用各自既有 bridges/16／17／18.png 第三格；与当前半程 34／35／36 共用同张图第一格的高度与宽度系数。该方案同时减少不同批次绘画、不同头顶色调和旧发梢轮廓混用。18 不恢复旧头顶覆盖。
- 重新生成图集、导出 59 帧并计算 157 对运动场。`.build/check-upward-registration.cjs` 检查三组完整／半程标定完全一致，且其余 56 帧裁切后的像素、尺寸和锚点不变，通过。`.build/check-upward-hair.ps1` 重算运动场后 39 组透明格式／画布边界检查通过。查看原生静态对照；没有进行实机鼠标视觉验收。
- 编译完成，正常关闭并备份至 `_archive/before-upward-registration-20260919-191223`，替换根目录程序并发送启动请求；部署脚本 `.build/deploy-upward-registration.ps1`，保留设置和摸头爱心功能。
- 验证限制：统一标定不代表各绘画内身体结构逐像素相同；残留发梢溢色、发色和体型切换的实际观感仍待用户复核，不能写成用户已验收。本轮没有宣称所有边缘色差已经消除。

## 2026-09-19 手动去溢色素材接入本地程序

- 用户要求：用户先说明“我手动去除了颜色溢出，你把新素材接入吧”，随后三次要求按既定流程完成替换；上一轮只做到重新导出素材，没有构建和替换程序，因此本轮从断点续做。
- 素材：沿用用户手改后的原路径三张图 `assets/animations/v6/bridges/16.png`（19:40:19）、`17.png`（19:41:27）、`18.png`（19:43:25），三张画布尺寸分别为 2061×763、2023×777、2172×724，与原素材尺寸一致。未新建或重命名素材，未改动 `prepare-v6.cjs` 的取材规则。
- 图集与过渡：上一轮已由 `.build/prepare-v6.cjs` 重新生成 `unified-sheet.png`（19:46:13）与 `atlas.json`（19:46:14），`.build/check-native-animation.ps1 export` 导出 59 帧原生位图（19:46:18），`.build/analyze-v6-motion.py` 重算 157 对双向运动场（19:46:51）。运动数据时间晚于原生导出与图集，顺序正确。
- 本轮核对：新增 `.build/check-manual-hair-frames.cjs`，与去溢色前快照 `_archive/before-manual-hair-20260919-194605/v6` 逐帧比较。结果：帧 0、8、16、17、18、34、35、36 使用新绘画，其余 51 帧的裁切像素、尺寸、headX、scaleHeight、widthFactor 全部一致；16／17／18 与各自半程 34／35／36 的标定完全相同；图集画布尺寸未变。这三张图同时提供当前正视（`bridges/17.png` 第一格，帧 0／8），因此正视颜色也随本次手改一起更新。
- 素材差异测量：新旧三张图轮廓 alpha 基本未变（分别只有 156／399／0 个像素获得透明度），人物剪影、比例和位置不变；但整体颜色比 9 月 18 日版本偏亮，人物内部不透明像素平均约 R+6.2／G+5.0／B+4.4，约 54% 的不透明像素通道差超过 2。这是数值测量结果，不代表用户看到的效果。如果用户本意只是清理发梢残留，那么这层整体提亮可能是编辑工具带来的，可用备份回滚。
- 对照图：`.build/make-manual-hair-compare.cjs` 生成 `.build/manual-hair-compare.png`（上排旧、下排新，列为左上／正上／右上），供用户自行查看。
- 构建与部署：`build.ps1 -OutputPath .build/manual-hair-candidate.exe` 编译成功；运行 `.build/deploy-manual-hair.ps1` 正常关闭旧程序、备份到 `_archive/before-manual-hair-20260919-194605`（含上一版程序、图集、源码脚本、文档与退出前后设置），替换根目录 `大肥鱼桌宠.exe`（35,112,448 字节，20:18:55）并发送启动请求。
- 部署读回：新版进程存在且响应；设置文件键值与关闭前完全一致，仅有可见位置 left 约 0.011 DIP 的窗口坐标舍入差，没有据此宣称位置逐字节一致。此读回不是视觉验收。
- 验证边界：本轮只做了图集一致性、轮廓 alpha 与颜色差异的数值核对，以及编译和部署读回；没有运行摸头回归、自动渲染对照或实机桌面手势验收。按用户此前要求未做视觉检查，发梢残留是否清除、三张图整体提亮的观感、正视颜色变化是否接受，均以用户实机体验为准，不能写成已验收。
- 未解决问题：三张图整体提亮是否用户本意（若否，可从 `_archive/before-manual-hair-20260919-194605` 回滚）；上方朝向切换时的发色连贯性与发梢残留；本轮未改动摸头、爱心粒子、张望、缩放和调速逻辑。

## 2026-09-19 用户二次细调三张图，再次接入

- 用户要求：用户表示“我又调整了一下这三张图的细节，再接入一次吧”，三张原路径素材分别于 20:24:19、20:25:48、20:27:11 被再次保存，仍为同一路径 `assets/animations/v6/bridges/16.png`、`17.png`、`18.png`。
- 回滚快照：先把上一版运行状态整体备份到 `_archive/before-manual-hair-details-20260919-202750`，内容为上一版程序、上一轮已接入的 `bridges/16–18.png`（19:40–19:43 版本）、当时运行中的 `v6/unified-sheet.png`、`atlas.json`、`motion-fields.gz`、`src/`、`开发文档.md` 以及关闭前后设置副本。该目录是本次修改的完整回滚点。
- 重新生成：`.build/prepare-v6.cjs` 重出图集（59 帧，格子 591×719，画布 2364×10785，与上一版完全一致）；`.build/check-native-animation.ps1 export` 导出 59 帧原生位图；`.build/analyze-v6-motion.py` 在受控提权下重算 157 对双向运动场（13,396,806 字节，OpenCV 5.0.0）。导出仍只有旧 FormattedText API 编译警告。
- 本轮核对：`.build/check-manual-hair-frames.cjs`（已支持指定基线目录）与上述回滚快照逐帧比较，通过：帧 0、8、16、17、18、34、35、36 使用新绘画，其余 51 帧的裁切像素、尺寸、headX、scaleHeight、widthFactor 全部一致；16／17／18 与半程 34／35／36 标定相同；图集画布尺寸与格子尺寸未变。
- 素材差异测量：`.build/measure-bridge-shift.cjs` 对比上一轮已接入版本，三张图均为轻微压暗，人物内部不透明像素平均为 16 号 R−1.35／G−0.80／B+0.08（16.5% 像素有变化）、17 号 R−1.77／G−1.04／B+0.10（31.4%）、18 号 R−4.36／G−2.57／B+0.26（59.2%）；三张图的轮廓 alpha 均无像素增减，剪影、比例、位置不变。这是数值测量，不代表用户看到的效果。
- 对照图：`.build/make-manual-hair-compare.cjs`（已支持指定基线与输出路径）生成 `.build/manual-hair-compare-2.png`，上排为上一版、下排为本次版本。
- 构建与部署：`build.ps1 -OutputPath .build/manual-hair-2-candidate.exe` 编译成功；`.build/deploy-manual-hair-details.ps1` 正常关闭旧程序、备份并替换根目录 `大肥鱼桌宠.exe`（35,220,992 字节，20:30:46），发送启动请求。
- 部署读回：新版进程存在且响应；关闭前后设置键值完全一致。用户在此期间移动并缩放过桌宠（left 1250.76／top 434.69／height 153.07），该位置与尺寸随设置保留，未被部署覆盖。读回不是视觉验收。
- 验证边界：本轮只做图集一致性核对、轮廓与颜色差异数值测量、编译和部署读回；按用户既有要求未运行摸头回归、自动渲染对照或实机视觉验收。三张图本轮细节调整的实际观感、发梢残留是否清除、正视颜色是否满意，均以用户实机体验为准。
- 未解决问题：三张图的最终色调是否满意；上方朝向切换时的发色连贯性与发梢残留；本轮未改动摸头、爱心粒子、张望、缩放与调速逻辑，也未改动 `prepare-v6.cjs` 取材规则。

## 2026-09-19 摸头姿势色调与底图不一致的定位与修正

- 用户反馈：用户实机询问“怎么摸头的色调变这么深了？”，属于视觉观感反馈，不是自动检查结论。
- 定位方法：新增 `.build/_frame-tone.cjs`，对当前版本、20:18 版本、用户第一次改图前（19:14）版本三份图集逐帧统计不透明像素的平均 RGB 与亮度。结论：摸头专用帧 50–58 在三份图集中**完全一致**（例如 f55 始终是 R108.1／G116.3／B145.6，亮度 117.2），摸头素材本身没有变深；变化的是底图正视帧 0／8，亮度由 19:14 版的 119.1 升到 20:30 版的 123.1。因此摸头保持姿势相对待机的色调差由 2.8 拉大到 5.9，观感上就像“摸头变深了”。
- 根因：摸头九宫格 `head-pet-original-tone-sheet.png` 是按用户第一次改图之前的底图色调校准的；用户先后两次手改 `bridges/16–18.png`，底图整体变亮而摸头素材没有同步，两者不再匹配。
- 变换求解：`.build/_tone-fit.cjs` 对用户手改前后的同一张正视原图（`bridges/17.png` 第一格）做逐通道线性拟合，得到用户编辑工具实际施加的色调变换：R 0.9838x+5.79、G 0.9953x+4.23、B 1.0062x+3.36，R² 分别为 0.9936／0.9957／0.9957，说明这是一次全局、逐通道的色调调整，而不是局部修边。
- 修正素材：`.build/prepare-head-pet-tone.cjs` 把同一变换应用到摸头九宫格，输出 `assets/animations/v6/head-pet-tone-matched-sheet.png`；原 `head-pet-original-tone-sheet.png` 保留为历史素材。处理只改 RGB 通道，alpha、姿势、轮廓、画布与位置均未改动，没有整图透明叠化，也没有重新生成绘画。
- 接入与重建：`prepare-v6.cjs` 的摸头素材改指向新文件；重新生成图集（59 帧，格子 591×719）、导出 59 帧原生位图、重算 157 对运动场（13,399,279 字节）。
- 核对结果：`.build/check-manual-hair-frames.cjs` 新增“期望变化帧列表”参数，与 `_archive/before-tone-match-20260919-204000` 比对，通过：只有 50–58 变了，其余 50 帧的裁切像素、尺寸、headX、scaleHeight、widthFactor 全部一致，16／17／18 与半程 34／35／36 标定仍一致。色调测量：摸头保持帧 f55 由 R108.1／G116.3／B145.6 提升到 R112.2／G120.0／B149.8（亮度 117.2 → 121.1），与正视 f0 的 R115.1／G121.9／B150.6（亮度 123.1）差距由 5.9 收到 2.0–3.0，恢复到用户第一次改图之前的关系（当时为 2.8）。`.build/_tone-compare.cjs` 生成 `.build/tone-compare.png` 供人工对照。
- 构建与部署：`build.ps1 -OutputPath .build/tone-match-candidate.exe` 编译成功；`.build/deploy-tone-match.ps1` 正常关闭旧程序、备份到 `_archive/before-tone-match-20260919-204000`（含 20:30 版程序、图集、运动场、`src/`、文档与设置快照），替换根目录 `大肥鱼桌宠.exe`（35,207,680 字节，20:40:27）并发送启动请求。
- 部署读回：新版进程存在且响应；关闭前后设置键值完全一致，用户位置与尺寸未被覆盖。读回不是视觉验收。
- 验证边界：本轮为数值定位、素材色调映射、编译与部署读回；按用户既有约定未运行摸头回归、自动渲染对照或实机视觉验收。“摸头是否还是偏深”以用户实机观感为准。
- 可选后续：如用户仍觉得偏深，可在此版本基础上再追加一次校正，让摸头姿势与待机正视色调完全对齐；若用户希望相反方向（把底图调回原色调、以摸头素材为准），可从 `_archive/before-tone-match-20260919-204000` 回滚底图改动。

## 2026-09-19 摸头色调二次校正：与待机完全对齐

- 用户要求：在上一轮部分校正之后，用户明确要求“再追加一次校正让摸头与待机色调完全一致”。本轮按此目标执行，覆盖上一轮“只恢复到既有关系”的保留态度。
- 做法：`.build/prepare-head-pet-tone.cjs` 在原有逐通道变换上追加一级常量偏移（相对第一次变换再 +3.1／+3.0／+2.7），等效变换为 R 0.9838x+8.89、G 0.9953x+7.23、B 1.0062x+6.06，把摸头九宫格里的正常站姿校准帧对齐到待机正视帧。仍只改 RGB，alpha、姿势、轮廓、画布与位置均未改动。
- 核对：`.build/check-manual-hair-frames.cjs` 与 `_archive/before-tone-match-2-20260919-205000` 比对通过，只有 50–58 变化，其余 50 帧的裁切像素、尺寸、headX、scaleHeight、widthFactor 全部一致；图集 59 帧、格子 591×719 未变。
- 色调读回：摸头校准帧 f50 由 R112.0／G118.9／B147.9（亮度 120.1）提升到 R115.0／G121.8／B150.5（123.0），与待机正视 f0 的 R115.1／G121.9／B150.6（123.1）相差 0.1；摸头其余姿势亮度 123.0–125.2，与待机差距不超过 2.1。用户第一次改图之前两者相差 2.8 且摸头整体偏暗，现在已完全对齐。
- 高光裁切核对：二级偏移后有约 2.6% 的通道样本达到 255；饱和阈值对应源图约 250 以上，属于原本就接近纯白的高光，被压缩幅度不超过 5，且暗部没有裁切（0 个样本）。没有为此改用非线性曲线或重绘。
- 重建与部署：导出 59 帧原生位图、重算 157 对运动场（13,401,131 字节）、编译 `.build/tone-match-2-candidate.exe`；`.build/deploy-tone-match-2.ps1` 正常关闭旧程序、备份到 `_archive/before-tone-match-2-20260919-205000`（含 20:40 版程序、图集、运动场、`src/`、文档与设置快照），替换根目录 `大肥鱼桌宠.exe`（35,172,352 字节，20:45:07）并发送启动请求。
- 部署读回：新版进程存在且响应；关闭前后设置键值完全一致，用户当时的位置与尺寸（left 1116.86／top 314.22／height 226.13）保留。读回不是视觉验收。
- 验证边界：本轮仍是数值核对、编译与部署读回；按既有约定未运行摸头回归、自动渲染对照或实机视觉验收。摸头与待机是否真正一致以用户实机观感为准。
- 未解决问题与回滚点：如果用户仍觉得不协调，可回滚到 `_archive/before-tone-match-2-20260919-205000`（部分校正版本）或 `_archive/before-tone-match-20260919-204000`（未校正版本）；也可反向把底图调回最初色调。

## 2026-09-19 摸头素材改由用户手改主文件驱动

- 用户要求：用户要求直接给出摸头那组图的路径，准备自己手动调节色调。
- 改动：把当前的 `head-pet-tone-matched-sheet.png` 复制为 `assets/animations/v6/head-pet-manual-sheet.png`（SHA256 完全相同），`.build/prepare-v6.cjs` 的摸头素材改读该文件，并在脚本内注明它是用户可手改的主文件、手改后不得再运行 `.build/prepare-head-pet-tone.cjs`（该脚本会从 `head-pet-original-tone-sheet.png` 重新生成，从而覆盖手改）。
- 回归核对：改名后重跑 `.build/prepare-v6.cjs`，`unified-sheet.png`（SHA256 6C41187B…）与 `atlas.json`（F1ADD6C8…）与改名前的哈希完全一致，说明本次改动不影响当前已部署程序使用的图集与运动场，无需重新编译或替换。
- 辅助工具：新增 `.build/make-head-pet-layout.cjs`，生成 `.build/head-pet-sheet-layout.png`（九宫格编号与用途对照图：50 正常站姿校准、51／52 进入、53／54 左扭（当前不播）、55 中央保持、56／57 右扭（当前不播）、58 恢复中间），供用户改图时定位。
- 后续流程：用户手改 `assets/animations/v6/head-pet-manual-sheet.png` 后，需重跑图集生成、原生帧导出、运动场计算，再编译与替换；本次没有改动程序、素材内容或注册数据。

## 2026-09-19 用户手改摸头九宫格后的接入

- 用户操作：用户表示“我改好了”，`assets/animations/v6/head-pet-manual-sheet.png` 于 20:50:48 保存新版本（手改主文件，管线只读此文件）。
- 素材差异测量：新增 `.build/measure-sheet-edit.cjs`，与手改前内容（`head-pet-tone-matched-sheet.png`，两者此前哈希相同）逐格比较。画布 1218×1291 未变；九格均为整体提亮，平均 R+2.36～+2.73、G+1.94～+2.12、B+1.85～+1.94；每格 41.4–43.7% 的不透明像素通道差超过 2，最大单通道差 29–40；九格的 alpha 变化像素全部为 0，说明轮廓、剪影与位置完全没动。
- 重建：`.build/prepare-v6.cjs` 重出图集（59 帧、格子 591×719、帧尺寸与之前一致）、`.build/check-native-animation.ps1 export` 导出 59 帧原生位图、`.build/analyze-v6-motion.py` 重算 157 对运动场（13,399,535 字节）。
- 核对：`.build/check-manual-hair-frames.cjs` 与 `_archive/before-manual-head-pet-edit-20260919-210500` 比对通过，只有 50–58 变化，其余 50 帧的裁切像素、尺寸、headX、scaleHeight、widthFactor 全部一致。
- 色调读回：待机正视 f0 亮度 123.1；摸头 f50 125.1、f52 126.8、f55 126.1、f58 125.5。手改前摸头与待机相差 0.1，现在摸头比待机亮约 2.0–3.7，属用户手动调整的结果，不再由脚本自动匹配。
- 对照图：`.build/make-tone-compare.cjs` 生成 `.build/tone-compare.png`（待机正视、手改前 f55、手改后 f55、手改后 f50）。
- 构建与部署：`build.ps1 -OutputPath .build/manual-head-pet-candidate.exe` 编译成功；`.build/deploy-manual-head-pet.ps1` 正常关闭旧程序、备份到 `_archive/before-manual-head-pet-edit-20260919-210500`（含 20:45 版程序、图集、运动场、`src/`、文档与设置快照），替换根目录 `大肥鱼桌宠.exe`（35,216,896 字节，20:53:13）并发送启动请求。
- 部署读回：新版进程存在且响应；关闭前后设置键值完全一致，用户当时的位置与尺寸（left 1127.37／top 407.35／height 153.66）保留。读回不是视觉验收。
- 验证边界：本轮只做素材差异测量、图集一致性核对、色调读回、编译与部署；按既有约定未运行摸头回归、自动渲染对照或实机视觉验收。
- 未解决与回滚点：摸头现在比待机亮 2.0–3.7，是否符合预期待用户实机确认；如需回到“完全一致”版本可用 `_archive/before-manual-head-pet-edit-20260919-210500` 回滚。

## 2026-09-19 整理发布内容并推送 GitHub

- 用户要求：用户确认素材定稿后要求“推上去吧”，把这一版实际用到的内容整理到一起并推到 GitHub。
- 远程与工作副本：私有仓库 `LdliMiku/fat-fish-desktop-pet`（`https://github.com/LdliMiku/fat-fish-desktop-pet.git`）。上一轮记录中的本地仓库 `C:\chatgpt\大肥鱼桌宠-v0.9` 已不存在，本次重新克隆到 `C:\chatgpt\大肥鱼桌宠-release`。推送分支 `main`，提交 `fac0ecf`，父提交 `0ac9848`；远程核对 `refs/heads/main` 已指向 `fac0ecf`。
- 入库内容：`大肥鱼桌宠.exe`（35,216,896 字节，即 20:53 部署版本）、`build.ps1`、`src/`（`AnimationRates.cs`、`NumericSettingRow.cs`、`PetAnimation.cs`、`PetApp.cs`、`PetMotionWarp.cs`、`PetSizeMotion.cs`、新增 `PetParticles.cs`、`app.manifest`）、`assets/pet-front.png`、`assets/particles/`（`pink-heart.png` 及源图）、`assets/animations/v6/`（`unified-sheet.png`、`atlas.json`、`motion-fields.gz`、`top-repairs.png/json`、`bridge-blinks.png`、`bridges/16–24.png`、新增手改主文件 `head-pet-manual-sheet.png`）、`assets/animations/v5/`、`.build/`（`prepare-v6.cjs`、`analyze-v6-motion.py`、`make-bridge-blink-source.cjs`、新增 `prepare-heart.cjs`）、`tests/`（`check.ps1` 与 `KeyframeChecks.cs` 更新为当前版本，新增 `ResizeMotionChecks.cs`，另含 resize 专项）、`docs/`、`AGENTS.md`、`README.md`、`开发文档.md`、`使用说明.md`。
- 排除与防误提交：未入库 `data/settings.txt`（用户设置）、`_archive/`（各版本备份）、`.build` 其余脚本与候选程序、`motion-inputs` 等缓存、`demo/`、根目录 AI 参考图 `Codex 图像 2026年9月17日 13_49_22.png` 与 `动画预览.html`。`.gitignore` 新增 `/_archive/`，并把停用的摸头素材（`head-pet-chest-*`、`head-pet-fixed-*`、`head-pet-original-*`、`head-pet-sheet.png`、`head-pet-tone-matched-sheet.png`、`head-pet-yaw-*`）列入忽略，当前使用的手改主文件不在忽略范围内。
- 可构建性核对：在克隆目录执行 `build.ps1 -OutputPath .build\verify-release.exe` 编译成功，产物大小与推送程序一致；随后删除该验证产物，未提交。没有在克隆目录运行桌面实机或摸头视觉验收。
- 已知限制：`tests/KeyframeChecks.cs` 内旧 `HeadPetChecks` 仍断言旧版 2.4 秒低头闭眼序列，因此不带 `export` 参数直接运行 `tests/check.ps1` 会失败；本次只走 export 分支导出原生帧，未修改该回归，已在提交说明中记录。
- 未做：未打新 tag（推送时仓库现有 `v0.1.0`、`v0.9.0`），未改程序集版本号（仍为 0.1.0.0），未同步 `codex/smooth-resize` 分支，未改动远程其他分支。

## 2026-09-19 删除远程 v0.9.0 tag

- 用户要求：删除仓库里的 v0.9。
- 目标确认：远程没有 v0.9 分支，只有轻量 tag `v0.9.0`，指向提交 e67498a「Package desktop pet v0.9 with configurable animation rates」；本地 `C:\chatgpt\大肥鱼桌宠-v0.9` 目录此前已经不存在，无需再处理。
- 操作：`git push origin :refs/tags/v0.9.0` 删除远程 tag，并在本地副本 `C:\chatgpt\大肥鱼桌宠-release` 用 `git tag -d v0.9.0` 删除同名 tag。`git ls-remote --tags` 复核，远程现在只剩 `v0.1.0`（含其注解对象）。
- 影响与恢复：e67498a 仍在 `main` 历史中，删除 tag 不丢失任何代码；如需恢复，执行 `git tag v0.9.0 e67498a` 后 `git push origin v0.9.0` 即可。
- 范围：本次只删除 tag，未改动 `main`、`codex/smooth-resize` 分支和任何提交内容。

## 2026-09-19 张望间隔改为 10–30 秒可调

- 用户要求：把待机动作里的左右转头间隔做成 10–30 秒可调。
- 旧实现：`IdleLook.Frequency` 是三档离散值（少／标准／多），`Interrupt` 里用 `20 + (20 + random*25) * factor` 计算等待，实际间隔为标准 40–65 秒、少 60–110 秒、多 30–42.5 秒，菜单入口为「张望频率」子菜单。
- 新实现（`src/PetAnimation.cs`）：改为 `IdleLook.Interval`（秒）+ 常量 `MinimumInterval=10`、`MaximumInterval=30`、`DefaultInterval=20`；`Interrupt` 用 `Interval * (0.88 + random*0.24)` 计算下一次等待，再夹紧到 10–30 秒，既保留轻微随机感又不越界。
- 设置兼容：新键 `idleLookInterval`；读取到旧键 `idleLookFrequency` 时按 少→30／标准→20／多→15 秒换算，且当新键已经读入时旧键不再覆盖（`intervalLoaded` 标记）。`ToLines` 只写新键，用户文件在下次保存时自动迁移。
- 界面（`src/PetApp.cs`）：移除「张望频率」子菜单，新增「调节张望间隔…」，打开 `ShowIdleLookWindow`；面板包含说明文字、`NumericSettingRow("张望间隔","秒",10,30,当前值,1)`、「恢复 20 秒」和「完成」按钮。滑块即时生效（同时 `pet.NotifyInteraction()` 让新间隔立刻开始计算）并保存，数值输入回车或失焦生效，超出 10–30 的输入会被拒绝并提示。
- 控件（`src/NumericSettingRow.cs`）：构造增加可选参数 `step`，`step>0` 时开启滑块按步长吸附并把 SmallChange／LargeChange 设为 step／step×5；原有百分比与倍率行不传该参数，行为不变。
- 检查：`.build/KeyframeChecks.cs` 的 `IdleLookChecks` 已更新——按 `Interval*0.88`／`*1.12` 判定冷却上下限、验证 10／20／30 秒三档首次触发时间落在各自窗口、验证上下限夹紧、旧频率映射、旧新键顺序、设置往返与新默认值。实际运行输出 `PASS: autonomous look timing, full poses, alternation, rates, interruptions, settings and 10-30s interval`。同一轮里旧的 `HeadPetChecks` 仍按过期断言报错 `Petting pose/duration/rate failed`，与本次改动无关，属已记录的待更新回归。
- 构建与部署：`build.ps1 -OutputPath .build/idle-interval-candidate.exe` 编译成功；`.build/deploy-idle-interval.ps1` 正常关闭旧程序、备份到 `_archive/before-idle-interval-20260919-231900`（含 `src/`、`KeyframeChecks.cs`、文档、程序与设置快照），替换根目录 `大肥鱼桌宠.exe`（35,218,432 字节，23:18:31）并发送启动请求。
- 部署读回：新版进程存在且响应；设置文件已从 `idleLookFrequency=1` 迁移为 `idleLookInterval=20`，其余键值与位置、大小完全保留。读回是设置迁移证据，不是界面或手感验收。
- 验证边界：倍率与张望间隔是两套独立设置，间隔按真实时间计时，不受「左右张望」倍率影响；本轮没有运行实机菜单操作，滑块手感和实际间隔观感待用户体验。
- 未做：本轮未推送 GitHub（`src/`、`.build/KeyframeChecks.cs`、`使用说明.md`、`开发文档.md` 已本地更新）；如需发布要另外执行整理与推送流程。

## 2026-09-19 正视图蓝色亮度对齐其他朝向

- 用户反馈：正视图的蓝色比其他动画亮，颜色出入有点大，要求把正视图蓝色稍微调低。
- 测量（新增 `.build/measure-direction-tone.cjs`）：逐帧统计不透明像素的整体 RGB、蓝色像素占比与蓝色像素的平均 R／B。当前正视帧 0／8 为 115.1／121.9／150.6（亮度 123.1），蓝色像素 77.5／139.5；用户手改 `bridges/16–18.png` 时未被改动的左／右／下方方向（帧 19、21–24、37–41）整体约 112.5／120.9／149.8，蓝色像素约 72.5／137.7。差异来自用户两次手改只覆盖了三张上方朝向原图，其余方向仍是 9 月 18 日色调。
- 修正素材：新增 `.build/prepare-front-tone.cjs`，以 `_archive/before-front-tone-20260920-001500/v6/bridges/17.png` 为固定输入，对**第一格**（正视图）按蓝度加权做逐通道下调：基础偏移 R−2.1／G−0.8／B−0.65，蓝度权重再叠加 R−2.0／G−0.5／B−0.8，权重为 `clamp((B−R−20)/40, 0, 1)`；第二、三格（半程与上方完整姿势）逐字节复制，alpha 完全不动，也没有重绘或缩放。脚本固定读取归档原件，可重复运行而不会叠加两次。
- 结果：正视帧 0／8 变为 112.0／120.9／149.6（亮度 121.5），蓝色像素 73.6／138.0；与其他朝向的整体差从 ΔR+2.6 收到 ΔR−2.2（落在左／右／下方各帧的自然范围内），蓝色像素的蓝通道与其他朝向平均只差 −0.16。
- 核对：`.build/check-manual-hair-frames.cjs`（指定期望变化帧 `0,8`）与 `_archive/before-front-tone-20260920-001500/v6` 比对通过——只有帧 0／8 变化，其余 57 帧裁切像素、尺寸、headX、scaleHeight、widthFactor 全部一致；三组完整／半程标定仍一致。`.build/make-tone-compare.cjs` 生成 `.build/tone-compare.png`（修正前正视图、修正后正视图、左视 19、右视 21）供人工对照。
- 重建与部署：重新导出 59 帧原生位图、重算 157 对运动场（13,400,066 字节）、编译 `.build/front-tone-candidate.exe`；`.build/deploy-front-tone.ps1` 正常关闭旧程序、备份到 `_archive/before-front-tone-20260920-001500`，替换 `大肥鱼桌宠.exe`（35,223,040 字节，23:29:07）并发送启动请求；进程存在且响应，设置未改动。读回不是视觉验收。
- 验证边界：改动只针对正视图，未触及 16／17／18 与 34／35／36（同一次手改让它们也偏亮），因此向上看时仍可能比左右方向略亮；用户若要一并处理可再执行一次同思路的校正。本轮未做自动动画回归或实机观感验收。
- 未做：未推送 GitHub；未修改摸头素材与张望间隔。

## 2026-09-19 接入 DeepSeek 对话窗口（方案 B）

- 用户选择：对话功能走"云 API + 聊天记录存本地"，并确认使用 DeepSeek 接口；语音朗读、语音输入、回复联动动作明确放到后续版本。
- 新增 `src/PetChat.cs`：
  - `ChatConfig`：接口地址、模型名、人设保存为 `data/chat-config.txt` 纯文本；API Key 用 `ProtectedData`（DPAPI，CurrentUser 作用域）加密后存 `data/chat-key.dat`，清空 Key 时删除该文件，任何情况下都不写明文；默认 `https://api.deepseek.com/v1` 与 `deepseek-chat`；人设提示词要求 1–3 句、合计 80 字内、不用列表／标题／Markdown，并允许提到"待在桌面上陪你"。
  - `ChatStore`：`data/chat-history.jsonl`，一行一条 `{time, role, text}`；启动加载最近 400 条用于显示，请求时只取最近 8 轮（16 条）作为上下文；支持整体清空。
  - `ChatEngine`：`HttpWebRequest` POST 到 `/chat/completions`（`Authorization: Bearer`、`stream:false`、temperature 0.8、max_tokens 400、连接与读写超时各 60 秒、关闭 `Expect: 100-continue`）；用 `JavaScriptSerializer` 解析 `choices[0].message.content`；把 401／402／429／超时／连不上分别翻译成中文提示；任何失败都退回 `LocalReply` 本地台词并附带原因，不抛出到界面。
  - 本地兜底台词：按"你好／摸头／夸奖／累了／告别／其它"六类关键词随机选句，均为短句。
- `src/PetApp.cs`：右键菜单「和我说话…」→ `ShowChatWindow`（可滚动气泡历史、输入框回车发送、发送／清空记录／接口设置按钮、底部状态行）；`SendChat` 在后台线程发请求，用 `Dispatcher` 回到 UI 线程追加气泡并写入历史，请求期间禁止重复发送；`ShowChatConfigWindow` 编辑接口地址／模型／Key（`PasswordBox`），可恢复默认地址或清空 Key；初始化时调用 `chat.Initialize(AppDomain.CurrentDomain.BaseDirectory)`。
- 构建：`build.ps1` 加入 `src\PetChat.cs`，并新增 `System.Web.Extensions.dll`、`System.Security.dll` 两个系统引用（本机已有，未引入任何第三方包）；仍是单 exe。
- 离线检查：新增 `.build/ChatChecks.cs` 与 `.build/check-chat.ps1`，用本地 `TcpListener` 模拟 OpenAI 兼容接口，覆盖配置与 Key 往返、Key 不以明文出现、历史写入／重载／上限／清空、无 Key 与 401 与连接失败的兜底、请求路径与请求体（模型、system 人设、最近上下文、新消息、stream:false）、401 错误翻译。实际运行 41 项全部通过（`PASS: chat engine checks`），不需要真实 API Key。
- 部署：`.build/deploy-chat-window.ps1` 正常关闭旧程序、备份到 `_archive/before-chat-window-20260919-233454`（含 `src/`、`build.ps1`、文档、程序与设置快照），替换根目录 `大肥鱼桌宠.exe`（35,240,960 字节，23:34:54）并发送启动请求；进程存在且响应，用户设置未改动。
- 验证边界：真实 DeepSeek 请求尚未用真实 Key 验证（需要用户填入 Key 后体验）；窗口布局、输入手感、回复质量与上下文长度感受均待用户实机确认。离线检查通过不等于联网可用。
- 数据位置与隐私：`data/chat-config.txt`、`data/chat-key.dat`、`data/chat-history.jsonl` 都在 `data/` 下，仓库 `.gitignore` 已忽略 `/data/`；本轮没有把任何 Key 写进项目文件。
- 未做：逐字流式显示、语音朗读、语音输入、回复联动角色动作、把对话接入角色状态；也未推送 GitHub。

## 2026-09-19 全部动画帧色调统一

- 用户要求：把各个动画的颜色统一，减少切换时的颜色跳变。
- 测量（新增 `.build/measure-frame-groups.cjs`）：按来源分组统计 59 帧的不透明像素平均 RGB 与亮度。修改前：正视（帧 0／8）121.5；v5 旧表（基础动作、眨眼、拎起、左右下方向完整帧）125.6，组内 122.1–128.9；闭眼桥接（帧 2、42–49）121.0；上方完整（16–18）124.1；上方半程（34–36）124.4；左右下半程（37–41）120.6；摸头（50–58）126.3，组内 125.1–127.4。最大组间差 5.7，方向切换时的"半程→完整"、跟随结束回正视、进入摸头等切换都能看出明暗跳变。
- 实现：`.build/prepare-v6.cjs` 在按原规则合成图集后新增一次色调统一——逐帧在最终画布上统计 alpha>200 像素的平均亮度，以第 0 帧（正视，用户刚确认过的参考）为目标计算修正量，单帧限制在 ±8 之内，然后对 alpha>8 的像素的 R／G／B 施加同一偏移，再写出 `unified-sheet.png`。只改 RGB，alpha 完全不动；裁切、缩放、headX、脚底注册等坐标数据不受影响。
- 选择只统一亮度而不是逐通道对齐：各姿势露出的皮肤、布料比例不同，逐通道拉平会强行改变色相；实测统一亮度后各组的通道差已降到 2.5 以内。
- 结果：59 帧亮度全部落在 121.4–121.8（组间差从 5.7 降到 0.4），平均修正量 3.2；分组通道值：正视 112.0／120.9／149.6，v5 旧表 112.7／120.8／149.4，闭眼桥接 114.5／119.8／147.5，上方完整 114.5／119.8／147.8，上方半程 114.1／120.4／149.0，左右下半程 112.1／120.8／150.0，摸头 113.1／120.3／149.3。
- 一致性核对：`atlas.json`（F1ADD6C8…）与修改前逐字节相同，说明只有像素被改，裁切、尺寸与注册数据完全没动；`.build/make-tone-unify-compare.cjs` 生成 `.build/tone-unify-compare.png`（上排修改前、下排修改后，列为上方 f17、右下 f24、摸头 f55）供人工对照。
- 重建与部署：重新导出 59 帧原生位图、重算 157 对运动场（13,395,461 字节）、编译 `.build/tone-unify-candidate.exe`；`.build/deploy-tone-unify.ps1` 正常关闭旧程序、备份到 `_archive/before-tone-unify-20260920-010000`，替换 `大肥鱼桌宠.exe`（35,269,120 字节，23:42:23）并发送启动请求；进程存在且响应，用户设置未改动。
- 自动生效：以后用户再手改任何一张素材，重跑 `prepare-v6.cjs` 就会自动重新平衡到正视色调，不需要再手工算偏移；正视帧本身是基准，所以改正视等于改整体基准。
- 验证边界：本轮为数值统一、图集一致性核对、编译与部署读回；没有做逐帧视觉走查或实机观感验收。"跳变是否消失、统一后是否偏暗"仍需用户实机判断。
- 未解决与后续：如需连色相也完全一致，可再做逐通道对齐（有改变姿态固有配色的风险）；逐字流式显示、语音朗读、语音输入、回复联动动作仍未实现；本轮未推送 GitHub。

## 2026-09-19 色调统一升级：整体与蓝色区域同时对齐

- 用户反馈：第一版色调统一后"感觉和之前没什么区别"。
- 部署核验（先排除没生效）：用反射读取运行中 `大肥鱼桌宠.exe` 的内嵌资源 `UnifiedSprites`，与磁盘 `assets/animations/v6/unified-sheet.png` 做 SHA256 比对，结果一致（146E879C…），确认程序确实在使用新图集，问题不在部署。
- 复测（新增 `.build/measure-frame-chroma.cjs`）：按 `B>R+20` 归为蓝色（头发与裙子）、`R>B+10` 归为肤色、其余归为白色，分别统计各帧各组的平均通道值。发现第一版只统一了整体亮度，蓝色像素仍差很多——蓝通道 131.6–141.7（约 10），红通道 69.5–76.4（约 7），而蓝色是画面主体，所以观感上"颜色还是不一样"。
- 实现升级：`.build/prepare-v6.cjs` 的统一环节由"每帧一个亮度偏移"改为"每帧、每通道的 基础偏移 + 蓝度加权偏移"：统计整体均值、蓝色像素均值、蓝度权重之和，用两道约束解 2×2 方程求 base 与 extra，再按 `offset = base + extra × clamp((B−R−20)/40,0,1)` 逐像素施加；蓝度权重是连续过渡，避免阈值附近出现可见色块边界；单像素总偏移仍限制 ±10，alpha 与几何完全不动。
- 结果：各组整体值 R112.0–113.1／G120.6–121.8／B149.6–151.2，蓝色像素 R71.7–75.3／G89.6–94.6／B133.4–138.8；相比第一版的 R111.1–118.0／G119.4–125.1／B147.0–154.2 明显收拢。`atlas.json` 与改动前逐字节相同，说明只有像素变化。
- 重建与部署：重新导出 59 帧原生位图、重算 157 对运动场（13,387,813 字节）、编译 `.build/tone-merge-candidate.exe`；`.build/deploy-tone-merge.ps1` 备份到 `_archive/before-tone-merge-20260919-235500`，替换 `大肥鱼桌宠.exe`（35,252,736 字节，23:48:13）并启动；进程存在且响应，替换后再次比对内嵌图集与磁盘图集，一致。
- 对照图：`.build/make-tone-unify-compare.cjs` 生成 `.build/tone-merge-compare.png`（上排本版之前、下排本版之后，列为上方 f17、右下 f24、摸头 f55）。
- 验证边界：仍是数值统一、图集一致性核对、内嵌资源核验、编译与部署读回，没有做视觉验收。如果观感上仍觉得跳，需要用户指出具体动作段（转身跟随、眨眼、摸头、拎起落地等），因为不同世代的绘画在轮廓、线条粗细和发型形状上本来就不同，那部分不是色调偏移能解决的。
- 用户体验反馈（实机，非自动检查）：用户在本版部署后回复"可以的确实好多了"，确认色调统一方向正确、跳变感明显减轻。这是本轮唯一的主观验收结论，来自用户本人，不代表所有动作段都已逐一看过；后续若再改素材或色调基准，应重新请用户确认。
- 未做：未做逐通道全量对齐（会改变各姿态固有配色）、未改素材源文件（全部在生成图集时统一）、未推送 GitHub。

## 2026-09-19 桌宠说话气泡

- 用户要求：加上交流气泡，不打开对话窗口也能看见她说了什么。
- 新增 `src/PetBubble.cs`：独立透明窗口（`WindowStyle=None`、`AllowsTransparency`、`ShowInTaskbar=false`、`ShowActivated=false`、`Focusable=false`、`SizeToContent`），内容为圆角粉色气泡加底部三角尾巴，与对话窗口里她那一侧同色。`BuildContent` 是静态工厂，检查脚本复用同一套视觉，避免"检查的和跑的不是同一个画面"。
- 时间与位置：`LifetimeFor` 把字数映射成 3–14 秒（`2.2 + 0.16 × 字数` 后夹紧）；显示 0.12 秒淡入，到时 0.3 秒淡出并隐藏；新消息替换内容并重新计时。`AnchorProvider` 由 `PetApp` 提供（角色可见区域顶部中心），100 毫秒跟随定时器重算位置，因此拖动和滚轮缩放时气泡都会跟住；位置按工作区夹紧，角色贴到屏幕顶部时气泡不会跑出屏幕。点击气泡打开对话窗口。
- 接入（`src/PetApp.cs`）：新增 `bubble` 字段与右键「显示对话气泡」开关（设置键 `bubbleEnabled`，默认开启、随设置保存）；`SendChat` 发送时先显示「……」，回复到达后替换成正文，请求失败时显示"唔……这次没答上来。"并把原因留在对话窗口状态行；`Closed` 时一起关闭气泡窗口。`build.ps1` 增加 `src\PetBubble.cs`。
- 检查：新增 `.build/BubbleChecks.cs` 与 `.build/check-bubble.ps1`，用真实 WPF 排版与渲染把气泡画成 PNG，断言字数→时长映射（下限 3 秒、上限 14 秒、随字数单调、空串取下限）、三种长度文本的宽度不超过上限、换行后高度增长、气泡主体为不透明浅粉、尾巴绘制在下方，并输出 `.build/bubble-preview.png` 供人工查看。实际运行 14 项全部通过（`PASS: bubble checks`）。
- 部署：`.build/deploy-bubble.ps1` 备份到 `_archive/before-bubble-20260920-001500`，替换根目录 `大肥鱼桌宠.exe`（35,257,344 字节，23:53:59）并发送启动请求；进程存在且响应，设置（含用户把张望间隔改成 10 秒）保留。
- 部署故障与修复：第一次替换失败于 `Copy-Item`——旧进程刚退出、映像仍被占用的瞬间；当时备份已完整写入、根目录程序未被破坏。已在部署脚本里加入"复制失败则每次间隔 400 毫秒重试，最多 10 次，仍失败才报错并不启动新版"的逻辑，重跑后成功。这个重试写法保留给后续部署脚本参考。
- 验证边界：气泡是真实桌面窗口，本轮没有做实机截图、点击或时长手感验收；布局位置、遮挡情况与显示时长是否合适都待用户体验。没有 API Key 时发一条消息也能看到气泡（显示本地台词），可以先用来确认外观。
- 未做：主动搭话（例如久坐提醒、欢迎回来）尚未实现；语音朗读、语音输入、回复联动动作仍未实现；本轮未推送 GitHub。

## 2026-09-20 主动搭话

- 用户要求：让她主动发一些话。
- 设计取舍：主动台词全部用本地预设句子，不调用 DeepSeek。理由是主动搭话需要立刻出现在气泡里，走网络会有几秒延迟、还要为没人在听的话付费；本地台词可以按时间、久坐、互动精确触发，且断网可用。想让她用 AI 生成内容时，仍然是在对话窗口里聊。
- 新增 `src/PetTalk.cs`：
  - 时段问候：0 深夜（23–5）、1 早上（5–11）、2 下午（11–17）、3 晚上（17–23）；启动后 4 秒说第一句，跨时段再补一句（至少隔 2 小时）。
  - 久坐提醒：距上次互动 ≥45 分钟提醒一次，两次提醒至少隔 1 小时。
  - 随机闲聊：基准间隔 `IntervalMinutes`（默认 30，范围 5–90），实际间隔为基准的 0.6–1.4 倍；每天上限 `DailyRandomLimit=16`。
  - 全局节流：任何两条主动台词至少隔 180 秒；互动回应（摸头／拎起／落地／点击）之间至少隔 12 秒，点击有 45% 概率不说话；对话窗口正在等待回复时（`chatBusy`）完全不打扰。
  - 台词库：四个时段的问候各 3 句、久坐 5 句、闲聊 12 句、摸头 3 句、点击 3 句、拎起 2 句、落地 2 句，全部 ≤24 字，保持活泼口语。
  - 时钟：`Tick(now, localNow, chatBusy)` 只依赖传入的单调秒数和本地时间，便于用假时钟做离线检查；`React(kind, now)` 处理互动回应；`Read`／`ToLines` 持久化 `talkEnabled` 与 `talkInterval`。
- 接入（`src/PetApp.cs`／`src/PetAnimation.cs`）：新增 20 秒节拍的 `DispatcherTimer` 调用 `Tick` 并把结果送进气泡；`NotifyInteraction()` 现在同时通知动画计时与搭话引擎（原 `pet.NotifyInteraction()` 的 13 处调用统一改为该包装方法）；拖动开始／落地／点击分别调用 `React("lifted"/"landed"/"clicked")`；`PetMotion` 新增 `Petted` 回调，在摸头手势触发时触发，`PetSpriteView` 暴露同名属性转发。右键菜单新增「主动搭话」开关与「搭话间隔…」面板（`NumericSettingRow`，5 分钟一档）。
- 语法坑记录：菜单里原来有个局部变量 `var talk = new MenuItem{...}`，与新的 `PetTalk talk` 字段重名，编译报 "MenuItem 未包含 Enabled 的定义"；已把菜单局部变量改名为 `chatMenu`。
- 离线检查：新增 `.build/TalkChecks.cs` 与 `.build/check-talk.ps1`，用固定随机种子和假时钟覆盖：关闭时全程静默、启动问候（首句时延、按时段选句、不重复）、时段边界（3/5/11/17/23 点）、久坐提醒（触发时刻 2760 秒、一小时最多一次）、长时间会话的闲聊次数与每日上限、任意两句之间的最小间隔、等待回复时沉默、互动回应与冷却、点击的随机沉默比例、所有台词库非空且 ≤24 字、设置默认值与上下限夹紧与往返。实际运行 29 项全部通过（`PASS: proactive talk checks`）。
- 默认值调整：最初把每日上限设为 10，检查显示 12 小时的会话会在第 10 句后彻底沉默，因此提高到 16，给典型工作日留出余量。
- 部署：`.build/deploy-talk.ps1`（沿用气泡版本加入的"复制失败自动重试"逻辑）备份到 `_archive/before-talk-20260920-003000`，替换根目录 `大肥鱼桌宠.exe`（35,264,000 字节，00:04:00）并启动；进程存在且响应，设置里已写入 `talkEnabled=1`、`talkInterval=30`，用户原来的位置、大小与张望间隔（10 秒）保留。
- 验证边界：规则逻辑有离线检查覆盖，但"什么时候说话不烦人"是主观体验，需要用户实机感受；截图、点击验收、多屏与长时间挂机表现都没测。启动问候在深夜（23–5 点）会说深夜台词，用户如觉得吵可以把「主动搭话」关掉或把间隔调大。
- 未做：AI 生成的主动搭话（例如结合上次聊天内容主动开口）、语音朗读、语音输入、回复联动动作仍未实现；本轮未推送 GitHub。

## 2026-09-20 说话气泡按参考图重做

- 用户要求：气泡 UI 有点丑，先用 GPT 生成参考美术资源，再照着做；尾巴跟随角色水平位置的想法保留；右键菜单保持原生；其它设置面板以后再说。
- 参考图：用户提供 1536×1024 的 PNG（`assets/ui/bubble-source.png`，来自微信临时目录，已复制进项目保存）。实测几何：内容 58..1517 × 226..811；气泡本体 58..1476 × 226..738（1418×512）；圆角半径约 185；尾巴在 x 674..852、尖端 y=811（比底边低 73）；鲸鱼在右下角，向右超出本体 41、向下超出 73。配色：填充 #cae6fd、描边 #85ace4、内圈与高光接近白色 #f2f9fe。
- 素材拆分（新增 `.build/prepare-bubble-ui.cjs`，只复制与修补像素，不重绘）：
  - `bubble-body.png`：① 尾巴凹口用相邻干净列整条覆盖（bandTop=底边-150 到尖端+10，再清掉底边以下残留）；② 鲸鱼覆盖的区域用**水平镜像**补成完整圆角，镜像会带来填色横向渐变错位，所以对"距左右轮廓超过 34 像素"的内部像素改用该行的均匀填色（从 x 780..980 采样），描边与白圈保留镜像结果。
  - `bubble-tail.png`：199×97，含尖端（贴图内尖端位置 99,83），可放在底边任意位置。
  - `bubble-whale.png`：368×352，来自未修改的原图，锚定在右下角（相对本体右边 -326、底边上方 278）。
- 绘制（`src/PetBubble.cs` 重写）：九宫格切片（源图 L58/T226/R1476/B738，四角 260×200）用 `CroppedBitmap` + `Image` 合成，`Stretch=Fill`：四角保持比例、四边与中心随文字拉伸；`Scale=0.13` 把源图像素换算成 DIP（约 1.5 像素描边）。文字内边距左 28／右 46／上 12／下 15（右侧多留一点避开鲸鱼），最大文字宽度 236。画布右侧留 10、底部留 14 像素容纳鲸鱼与尾巴外溢。
- 尾巴跟随：`TailLeftFor(anchorX, bubbleLeft, bodyWidth)` 把"角色中轴"换算成贴图左边缘位置并夹在 `[CornerWidth, bodyWidth - CornerWidth - 尾巴宽度]` 之间；`Reposition()` 每 100 毫秒重算一次，所以拖动、滚轮缩放、被屏幕边缘挤压时尾巴都会重新指向角色。
- 换装路线：`BuildContent(message, tailLeft)` 拆成"皮肤参数 + 布局逻辑"两层，皮肤常量（Scale、切片矩形、内边距、配色）集中在文件顶部，以后换风格只改常量。
- 离线检查：`.build/check-bubble.ps1` 与 `.build/BubbleChecks.cs` 从 14 项扩到 19 项——时长映射、三种长度文本的宽度上限、换行后高度增长、短文本不会更宽、填充为不透明淡蓝（Pbgra32 字节序 B/G/R/A，检查时按 B 通道判断）、左边缘有蓝色描边、底边下方有尾巴贴图、右下角有鲸鱼贴图，以及尾巴跟随的四条断言（居中时尾巴尖正对角色、左右边界夹紧、随角色水平移动）。实际运行全部通过，并输出 `.build/bubble-preview.png`（三张预览分别模拟角色在 22%／50%／82% 位置）。
- 语法记录：`CroppedBitmap` 需要整数矩形；最初用 `ImageBrush` + 绝对 `Viewbox` 的写法在 `RenderTargetBitmap` 下没有画出任何切片（预览只剩文字和鲸鱼），改成 `CroppedBitmap` 后正常；另外 C# 5 不允许 `var a = 1, b = 2;` 这种多声明写法（CS0819），检查脚本里踩到一次。
- 部署：`.build/deploy-bubble-skin.ps1` 备份到 `_archive/before-bubble-skin-20260920-010000`（含 src、assets/ui、素材脚本、文档、程序与设置快照），替换 `大肥鱼桌宠.exe`（36,855,296 字节，00:34:03）并启动；进程存在且响应，内嵌资源列表中已包含 BubbleBody／BubbleTail／BubbleWhale。
- 验证边界：气泡外观是用真实 WPF 渲染路径出的预览图（和程序同一套代码），但没有做桌面实机截图；实际显示尺寸、鲸鱼在她头顶时的观感、跟随手感都待用户体验。右键菜单按要求保持原生风格；对话窗口与设置面板的换皮按用户要求延后。
- 未做：其它界面换皮、AI 主动搭话、语音朗读、语音输入、回复联动动作；本轮未推送 GitHub。

## 2026-09-20 气泡：竖线修复、高度跟随文字、参数改为素材自动生成

- 用户反馈：① 气泡右端有一条竖线，把角色放到左边后线也跑到左边；② 一句话的气泡太高，高度应随对话长度变化；③ 明天准备换一版气泡素材，要求先把气泡逻辑做好，明天直接换源即可。
- 竖线定位与修复：旧的尾巴切片（`.build/prepare-bubble-ui.cjs`）上边界取的是"底边线上方 10 像素"，把气泡底边的一条带一起切进了贴图；贴回被修补过的本体时两者对不齐，在贴图左右边缘形成一条竖线，尾巴移动时竖线跟着走——这与用户观察到的现象吻合。修正：切片上边界改为底边线本身（`top = bottomLine`），高度 = 尖端 + 9 像素的柔和阴影；`PetBubble` 里对应常量 `TailTipAboveBottom` 归零。
- 新增检查：`.build/BubbleChecks.cs` 增加"底边上方无垂直接缝"——取本体底边上 5 像素一行、在尾巴贴图范围内只统计浅色像素的 B 通道极差，要求 ≤12。修复前极差 67（明显接缝），修复后为 3。
- 高度跟随文字：原先四角切片（200 源像素 × 0.13）把最小高度撑到约 64 DIP，一行短句也是那么高。现在缩放由素材脚本自动推导——沿本体中部行扫描"描边 + 白圈"的厚度 `band`，取 `scale = 3 / band`（让描边显示约 3 DIP）；当前参考图 `band≈35`，得 `scale = 0.086`，四角约 19.8 DIP。`BuildContent` 的高度公式改为 `max(2×角高 + 6, 上留白 + 文字高 + 下留白)`：一行约 48、两行约 69、三行约 90。同时删掉了之前为压高度临时加的"四角竖直压扁 0.62"系数，四角恢复为正圆角。
- 换素材流程（本次的核心目的）：`.build/prepare-bubble-ui.cjs` 除生成 `bubble-body.png`／`bubble-tail.png`／`bubble-whale.png` 外，现在还会自动量出并写出 `assets/ui/bubble-meta.json`：`scale`、本体矩形、四角尺寸、尾巴贴图尺寸与尖端坐标、右下角装饰（不存在时 `shell: null`）、文字内边距、最大文字宽度、文字颜色（取描边色 50%）。`src/PetBubble.cs` 新增 `BubbleSkin.Load()` 解析该 JSON（嵌入资源名 `BubbleMeta`），任何字段缺失或解析失败都退回内置默认值，所以素材脚本坏了也不会让气泡崩掉。以后换素材：覆盖 `assets/ui/bubble-source.png` → 跑脚本 → 跑检查看预览 → 编译 → 部署，全程不改 C#。
- 尾巴跟随的可见性：气泡原本始终以角色中轴居中，尾巴永远停在正中间，用户因此反馈"尾巴没变"。现在气泡整体朝屏幕中心方向偏移最多 18% 宽度（`TailBias`），角色在左半边时尾巴落在气泡右侧、右半边时落在左侧，跟随一眼可见；气泡仍在她头顶附近，且会被工作区夹住。
- 检查与预览：`.build/check-bubble.ps1` 现在 20 项（含时长映射、宽度上限、换行增高、短句不更宽、填充色、描边、尾巴在底边下方、鲸鱼在右下角、无垂直接缝、尾巴跟随四条断言），全部通过；`.build/bubble-preview.png` 更新为"角色在 22%／50%／82% 位置"的三张对比图。
- 部署：`.build/deploy-bubble-skin2.ps1`（沿用复制重试逻辑）备份到 `_archive/before-bubble-skin2-20260920-020000`，替换 `大肥鱼桌宠.exe`（36,852,736 字节，00:44:04）并启动；进程存在且响应。
- 验证边界：以上仍是数值检查与真实 WPF 渲染预览，没有桌面实机截图；新气泡在她头顶的实际观感、鲸鱼大小、尾巴跟随的幅度是否合适都待用户确认。素材缩放是按描边厚度自动推的，若明天的新素材描边特别粗或特别细，可以手动改 `bubble-meta.json` 里的 `scale` 覆盖。
- 未做与下步：明天换素材后需要重跑素材脚本、检查、编译与部署；其它界面换皮、AI 主动搭话、语音朗读、语音输入、回复联动动作仍未实现；未推送 GitHub。

## 2026-09-20 气泡改为侧边出现（手绘素材）+ 说话前转头

- 用户要求：① 尾巴逻辑换掉，不再左右滑动；② 气泡改到角色**侧边**出现（参照新给的手绘风格合成图）；③ 气泡大小随文本量变化；④ 尾巴位置固定；⑤ 由"离桌面哪边边缘远"决定放左边还是右边，竖向不用管；⑥ 触发气泡前朝气泡那一侧转头，并**持续到气泡消失**。
- 新素材：用户提供手绘风格参考（1774×887，纸纹奶白填充、浅蓝双层描边、左上小笔触、右上爱心、右下小鲸鱼与尾巴），保存为 `assets/ui/bubble-source-handdrawn.png`，并作为当前 `bubble-source.png`。合成图里的角色只是示意，不进入气素材。
- 素材脚本适配（`.build/prepare-bubble-ui.cjs`）：
  - 尾巴检测：改为"底边下方连续下凹超过 12 像素、且成片至少 24 列"才算尾巴；多个区段时优先取靠近中间的（老素材尾巴在中间、外部鲸鱼在右侧），中间没有就取最右侧（新手绘素材的尾巴长在右下角）。这版实测尾巴在 x 1534..1635、尖端比底边低 35 像素。
  - 底边线：手绘轮廓抖动大，逐像素众数会被带偏，改为按 8 像素分箱取最多的一箱、再取箱内中位数（实测 767）。
  - 外挂装饰：改为自动检测"轮廓之外、且不属于尾巴的实心像素"，并要求长宽都超过 60 像素才算成块装饰。手绘素材的鲸鱼画在气泡**内部**，因此检测不到，元数据写成 `shell: null`，运行时跳过该层（旧素材的外部鲸鱼仍能被识别）。
  - 内边距改为 `corner×scale×1.1 + 6` 起算（实测左 18／右 18／上 14／下 15），文字最大宽度改为 280。
- 绘制与布局（`src/PetBubble.cs`）：
  - 移除尾巴跟随逻辑（`TailLeftFor`、`TailBias`），尾巴固定在朝向角色那一侧的底角：气泡在角色左侧时落在右下角，镜像后自然变成左下角。
  - 新增纯函数 `PlacementFor(character, bubble, workArea, out left, out top, out onRight)`：`onRight = (area.Right - character.Right) >= (character.Left - area.Left)`（哪边空间大放哪边）；横向留 `SideGap = 10` 的间隙；竖向让尾巴尖落在角色高度的 42%（`TailAnchorHeight`）；最后夹进工作区。
  - `AnchorProvider` 由"一个点"改为"角色可见矩形"，`Reposition` 在角色换到桌面另一半、需要换边时重建内容（重新生成镜像版）并重新排版。
  - 镜像只作用于气泡图形（图形放在带 `ScaleTransform(-1,1,...)` 的子画布里），文字不翻转。
- 说话前转头：`PetSpriteView` 新增 `ForceLook(double x)`（-1 左、1 右、NaN 不强制），在视线更新里优先于鼠标偏移；`PetApp.ShowTalkLine` 先调用 `bubble.PreviewOnRight()` 判断气泡将落在哪一侧并立即转头，320 毫秒后才弹出气泡；`bubble.Hidden` 事件把强制视线设回 NaN，恢复正常跟随。所以转头从气泡出现之前开始，一直保持到气泡消失；点气泡、清空或关闭气泡都会立即恢复跟随。
- 检查：`.build/BubbleChecks.cs` 更新为 21 项——新增"尾巴在右下角""镜像后尾巴在左下角""角色贴左边缘时气泡在她右侧""贴右边缘时在左侧""气泡不超出工作区""顶部夹紧"，移除旧的尾巴跟随断言；预览图第三张改为镜像版。`.build/check-bubble.ps1` 实测全部通过。
- 部署：`.build/deploy-side-bubble.ps1` 与 `.build/deploy-side-bubble2.ps1`（第二次是因为元数据里内边距修正后需要重新编译），备份 `_archive/before-side-bubble-20260920-033000`，替换 `大肥鱼桌宠.exe`（37,038,080 字节，01:12:02）并启动；进程存在且响应。
- 验证边界：仍是真实 WPF 渲染预览与数值断言，没有桌面实机截图；侧边气泡的实际观感、与角色的间隙、转头幅度与保持时间都待用户体验。手绘素材的笔触在长句时会被横向拉伸（预览第三张可对比），这是九宫格方案的固有取舍。
- 未做：其它界面换皮、AI 主动搭话、语音朗读、语音输入、回复联动动作；未推送 GitHub。

## 2026-09-20 聊天窗口：她的回复改用手绘气泡 + 布局收紧

- 用户反馈：聊天界面布局优化一下，"左边空太多了"；并问聊天里的回复气泡能不能也用桌面那套手绘气泡。
- 实现（`src/PetApp.cs`）：
  - `AddChatBubble` 里她那一侧改为 `PetBubble.BuildContent(text)`（复用桌面气泡的手绘素材、九宫格与居中文字），用户那一侧保持浅蓝圆角气泡；用户气泡最大宽度 320 → 400，圆角 8 → 10，内边距略增。
  - 布局收紧：窗口内容内边距 14 → 10，日志区 `ScrollViewer` 留白 8 → 6，消息之间的下边距 8 → 4，标题与输入区的外边距也各减 2。之前"左边空太多"来自窗口内边距叠上日志留白，消息距离左边缘约 30 像素；现在约 16 像素。
  - 桌面气泡与聊天窗口现在共用同一套视觉：修改素材（`bubble-source.png` + `prepare-bubble-ui.cjs`）会同时影响两处。
- 布局检查工具：新增 `.build/ChatWindowMock.cs` 与 `.build/make-chat-mock.ps1`，用与程序相同的控件结构（她那一侧直接调用 `PetBubble.BuildContent`）渲染 `.build/chat-mock.png`，以后调聊天窗口布局可以先看这张图。
- 部署：`.build/deploy-chat-ui.ps1` 替换 `大肥鱼桌宠.exe`（37,431,808 字节，08:20:35）并启动；进程存在且响应。
- 验证边界：`chat-mock.png` 是用同一套 WPF 控件渲染的静态截图，不代表实机窗口在真实窗口管理器中的观感；消息很多时的滚动、暗色壁纸下的对比度仍待用户确认。
- 未做：其它设置面板换皮、AI 主动搭话、语音朗读、语音输入、回复联动动作；未推送 GitHub。

## 2026-09-20 聊天窗口与气泡的后续微调

- 用户逐条反馈：① 聊天窗口左侧大标题「和大肥鱼说话」取消；② 聊天里她的气泡方向反了；③ 气泡里的文字要居中、不要大片空白，紧凑一点。
- 改动（`src/PetApp.cs`）：删掉标题 `TextBlock`；她那一侧改为 `PetBubble.BuildContent(text, true)`（镜像版，尾巴朝左）；用户那一侧的气泡文字加 `TextAlignment = Center`；消息外层 `Border` 的 `HorizontalAlignment` 改为"她左、用户拉伸"（之前默认拉伸会把固定宽度的气泡居中，看起来像右对齐）。
- 改动（`.build/prepare-bubble-ui.cjs`）：内边距公式收紧为 `left = corner.left×scale×0.9+4`、`right = min(corner.right,260)×scale×0.9+6`、`top = corner.top×scale×0.7+3`、`bottom = corner.bottom×scale×0.45+4`；本素材实测 14/29/11/20（原来 18/35/14/30），两行文字的气泡高度 86 → 73。
- 气泡内部细线的成因与修复：用户截图显示气泡里有横竖两条淡淡的线，位置正好在九宫格切片边界上（横线在上/下切片交界、竖线在右侧切片交界）。根因是九宫格必然把中间与边缘拉伸、四角保持原样，而手绘纸纹是非均匀颗粒，拉伸区与未拉伸区的纹理密度对不上，于是在边界出现一条"密度台阶"。修法：素材脚本把"与底色通道差 ≤18 且 alpha ≥250"的内部像素统一压成同一个底色（实测约 86 万像素，取中段采样的平均值 #f6f4f3），描边、鲸鱼、爱心与柔和边缘保持原样——填充变成纯色后，切片边界不再有可见差异；底边一行填充色极差由 25 降到 10，`.build/check-bubble.ps1` 21 项通过。副作用是气泡内部的纸纹颗粒被抹平，如果需要保留纹理，可改用"中心区域平铺原始纹理"的方案（见下条未做项）。
- 检查工具：`.build/make-chat-mock.ps1` 渲染 `.build/chat-mock.png`（与程序相同的控件结构，她那一侧直接调用 `PetBubble.BuildContent`），本轮用它核对标题删除、镜像方向与居中效果。
- 部署：`.build/deploy-chat-ui.ps1`、`.build/deploy-seam-fix.ps1`、`.build/deploy-compact.ps1` 依次替换 `大肥鱼桌宠.exe`（最后为 36,345,856 字节，08:29:10），进程存在且响应。备份目录沿用 `_archive/before-bubble-fix-20260920-040000`（每次替换前会刷新为上一版）。
- 验证边界：均为真实 WPF 渲染预览与数值断言，没有实机截图；标题删除后的窗口观感、气泡紧凑度与纯色填充是否符合预期都待用户确认。
- 未做：保留纸纹的"中心平铺纹理"方案、其它设置面板换皮、AI 主动搭话、语音朗读/输入、回复联动动作；未推送 GitHub。

## 2026-09-20 气泡接缝彻底消除、装饰恢复、文字完全居中、聊天留白

- 用户反馈四条：① 文字要上下左右都居中；② 新版气泡边缘不够圆润、爱心变形；③ 细线在白底看不出、放在桌面能看出；④ 聊天气泡贴左边界太紧。
- ① 居中：原来 `textLeft` 取自 `PadLeft`，而右下角切片很宽导致左右 padding 不对称，视觉上偏左。改为按气泡中线计算：`contentWidth = max(textWidth, width −(PadLeft+PadRight))`、`textLeft = (width − contentWidth)/2`、`textTop = max(PadTop,(height − textHeight)/2)`。
- ② 圆润与装饰：上一版"通道差 ≤18 就压平"的规则把爱心内部的浅粉填色和描边附近的抗锯齿过渡也一起抹了。现在先用"与底色通道差 >14"判定墨色像素，再向外膨胀 3 像素生成保护掩膜，只压平掩膜外的纯底色——压平像素由 86 万降到 53 万，爱心、鲸鱼和圆角过渡保持原样。
- ③ 细线：位置正好在九宫格切片边界，且颜色是"底色与背景的混色"（约 222,224,224），说明是拉伸切片在线性采样时把边界外的透明像素混了进来——这也解释了为什么白底看不出、深色桌面明显。修复：几何全部取整到整像素（四角尺寸与气泡宽高都 `Math.Round`），并让每条切片向外多取 2 像素（`Slice` 内的 Bleed 逻辑：源矩形按缩放比例外扩、目标矩形同比例外扩，并用负 Margin 抵消偏移），使切片边缘的像素始终是邻居的内容，混色也混不到背景。预览背景改为深色（#2B4A52）后确认细线消失；这条经验也写进了检查脚本（预览用深色，浅色会掩盖问题）。
- ④ 聊天留白：日志区 `ScrollViewer` 留白 6 → 10，她的消息加 6 像素左边距、用户的消息加 6 像素右边距，文字与窗口边界之间保持距离（此前只有约 6 像素）。
- 检查与部署：`.build/check-bubble.ps1` 21 项通过；`.build/deploy-polish.ps1` 替换 `大肥鱼桌宠.exe`（36,437,504 字节，08:40:10）并启动，进程存在且响应；`.build/make-chat-mock.ps1` 重新渲染 `.build/chat-mock.png` 供核对。
- 验证边界：预览是真实 WPF 渲染 + 深色背景，但没有实机截图；圆润度、爱心是否复原、桌面上的细线是否彻底消失仍需用户确认。
- 未做：恢复纸纹（可改为中心 1:1 平铺）、其它设置面板换皮、AI 主动搭话、语音朗读/输入、回复联动动作；未推送 GitHub。

## 2026-09-20 v0.2 发布准备

- 用户要求：把当前版本推到 GitHub，**不要上传聊天记录和 API Key**，把这一版定为 v0.2，并更新开发文档与 README。
- 版本号：`src/PetApp.cs` 的 `AssemblyVersion` 与 `src/app.manifest` 的 `assemblyIdentity` 由 0.1.0.0 改为 **0.2.0.0**；`使用说明.md` 标题改为 v0.2 版；README 全面改写为 v0.2（新增摸头回应与爱心粒子、左右张望、对话窗口与说话气泡、主动搭话、色调统一、平滑缩放等功能说明，更新项目结构与验证命令）。程序按新版本号重新编译并替换本地运行版本。
- 隐私边界：聊天记录 `data/chat-history.jsonl`、API Key `data/chat-key.dat`、接口配置 `data/chat-config.txt`、程序设置 `data/settings.txt` 全部位于 `data/` 目录，仓库 `.gitignore` 自 v0.1 起就包含 `/data/`，本次推送前再次核对 `git status` 确认它们未被纳入。
- 入库内容（相对上一版新增或更新）：`src/PetChat.cs`、`src/PetBubble.cs`、`src/PetTalk.cs`、`assets/particles/`、`assets/ui/`（参考图、本体、尾巴、装饰、`bubble-meta.json`）、`.build/prepare-bubble-ui.cjs`、`tests/check-chat.ps1`+`ChatChecks.cs`、`tests/check-bubble.ps1`+`BubbleChecks.cs`、`tests/check-talk.ps1`+`TalkChecks.cs`、更新后的 `assets/animations/v6`（图集、运动场）与 `src/`、`README.md`、`使用说明.md`、`开发文档.md`；`.gitignore` 增加 `/_archive/` 与停用的摸头素材规则。
- 未做：没有把 `_archive/` 备份、`.build` 候选程序与缓存、`demo/` 目录、根目录参考图与 `动画预览.html` 纳入仓库；未改动 `codex/smooth-resize` 分支。

## 2026-09-20 气泡尾巴闭合、鲸鱼保留、文字居中

- 用户反馈：① "尾巴没闭合"；② 文字要居中放在气泡里面。
- 尾巴断开的原因：上一版把尾巴抠成独立贴图（从底边线往下切），又把本体缺口修补成直边，于是尾巴的两条描边和气泡底边断成了两截。
- 修正方案（`.build/prepare-bubble-ui.cjs`）：不再切开、也不再修补本体——`bubble-body.png` 直接输出原图；把右下角切片放大到包住尾巴和整块鲸鱼装饰（`corner.right = max(圆角推导值, bodyRight - 装饰左边界 + 30)`、`corner.bottom = max(圆角推导值, 内容底部 - 装饰顶边 + 20)`，并限制在本体的 62%／70% 以内）。本素材实测 `corner = {left 113, right 771, top 113, bottom 353}`、`body.bottom` 取到尾巴尖端 802，于是尾巴、鲸鱼、爱心、水花全在同一个不缩放的切片里，描边自然闭合、也不会被横向拉伸。
- 绘制简化（`src/PetBubble.cs`）：删掉独立尾巴贴图与装饰贴图的绘制（`BubbleTail`／`BubbleWhale` 仍由脚本输出，留作素材备份，但运行时不加载），九宫格 + 文字即可；`padding.right` 改为按 `min(corner.right, 320)` 计算，避免因为右下角切片很宽而把文字挤到左边。
- 文字居中：`TextBlock` 设 `TextAlignment = Center`，宽度取 `max(文字宽度, 气泡可用宽度)`，纵向位置按 `max(PadTop, (height - 文字高) / 2)` 均分留白，所以短句在偏宽的气泡里也是居中的。
- 检查与部署：`.build/check-bubble.ps1` 21 项通过（含"左下角无接缝"、"尾巴在正确底角"、"气泡在工作区内"、"镜像后装饰翻到左侧"等）；`.build/deploy-bubble4.ps1` 替换 `大肥鱼桌宠.exe`（37,431,808 字节，08:15:36），进程存在且响应。
- 验证边界：仍是真实 WPF 渲染预览与数值断言，没有桌面实机截图；居中后的观感、鲸鱼大小是否合适仍待用户确认。
- 未做：其它界面换皮、AI 主动搭话、语音朗读、语音输入、回复联动动作；未推送 GitHub。

## 2026-09-20 手绘气泡的鲸鱼与尾巴修好、转头逻辑删除

- 用户反馈：气泡不如意——少了标志性小鲸鱼、尾巴"稀碎"；对话转头逻辑做不出来就删掉；另外气泡离角色太远，要贴近一点。
- 排查：① 小鲸鱼那一块（鲸鱼＋水花＋小爱心）在手绘素材里宽约 721 像素、高约 298 像素，而当时的四角切片只有 113 像素，装饰落进会被横向拉伸的中间/底边切片 → 被拉成看不出形状；② 尾巴贴图 122×44 像素，在 scale 0.1 下显示为 12×4 像素，太小所以"稀碎"；③ 转头逻辑：`ForceLook` 与延时显示都已接好，但用户实机没有感知到，按要求删除。
- 素材脚本（`.build/prepare-bubble-ui.cjs`，本轮重写）：
  - 内部装饰块检测：在右下角窗口（占本体宽 45%、底边以上 50% 范围内、离开轮廓 30 像素）按"与同一行填充色的通道差 > 28"筛出实心像素，成片超过 1200 像素即视为装饰块，向外留 8–10 像素边距后抠成 `bubble-whale.png`；本素材实测 (958,469)-(1679,767)，即 721×298。
  - 本体补齐：尾巴缺口用相邻列整条覆盖；装饰块区域用水平镜像覆盖，并把"距左右轮廓 34 像素以内"之外的部分换成本行均匀填色，避免镜像带来的横向渐变接缝。
  - 几何：底边线用 8 像素分箱众数的中位数；尾巴按"连续下凹 > 12 像素、成片 ≥ 24 列"识别（本素材在 x 1534..1635、尖端低 35 像素）；四角尺寸改为分左右上下四值（`corner: {left,right,top,bottom}`），便于以后按装饰分布单独调；缩放默认由"描边＋白圈厚度 ≈ 3 DIP"推导，可用 `--scale=` 覆盖；四角也可用 `--corner=左,右,上,下` 覆盖。
- 绘制（`src/PetBubble.cs` 重写）：九宫格改为支持四边不同尺寸的切片；装饰贴图按固定尺寸叠在右下角；尾巴按 `tail.side` 贴在同侧底角（镜像时随图形一起翻到另一侧）；最小宽度保证能容纳装饰贴图，避免贴图探出气泡左边缘。
- 贴合距离：`PlacementFor` 里先把角色可见矩形按 14% 内缩（素材四周本来就有透明留白），再留 4 像素间隙，视觉上比之前贴近很多。
- 删除的代码：`PetSpriteView.ForceLook`、`OnRendering` 里的强制视线分支、`PetApp` 的 `talkShowTimer`/`pendingLine` 延时显示与 `bubble.Hidden` 恢复逻辑。
- 检查：`.build/BubbleChecks.cs` 适配新结构（四边角尺寸、装饰贴图、底边接缝改到"未被装饰覆盖的左侧半边"并只看纸面填充），21 项全部通过；预览图三张分别是短句、两行、三行镜像版。
- 部署：`.build/deploy-bubble-fix.ps1`，备份 `_archive/before-bubble-fix-20260920-040000`，替换 `大肥鱼桌宠.exe`（37,144,064 字节，01:18:55）并启动；进程存在且响应。
- 验证边界：真实 WPF 渲染预览 + 数值断言，没有桌面实机截图；鲸鱼大小是否合适、贴合距离是否舒服、长句笔触拉伸是否可接受，都待用户体验。
- 未做：其它界面换皮、AI 主动搭话、语音朗读、语音输入、回复联动动作；未推送 GitHub。
