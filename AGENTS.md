# 项目开发约定

开始开发前阅读根目录 `开发文档.md`，以代码和实际素材核对当前状态。

每次修改功能、动画、素材或构建流程后，必须同步更新 `开发文档.md`：说明改动目的、涉及文件、验证结果及未解决问题。用户体验反馈与自动测试结果分开记录，不得把自动检查通过写成用户已验收。

保持用户已确认的颜色、体型、头发留白、呆毛方向、动作幅度和可调速功能。姿势之间禁止整图透明叠化；每个输出画面只采样一套完整角色绘画。眨眼只能修改眼睛区域，不得替换整体轮廓。

大幅姿态变化应使用实体关键帧和中间姿势；几何插值只辅助连接相邻绘画。素材更改后重新导出原生帧并计算运动场，验证后再替换运行程序。

保留用户设置。替换可执行文件前正常关闭旧程序并备份；不强制终止无关进程。提交和上传前确认没有测试截图、缓存或私人设置被意外纳入。
