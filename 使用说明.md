# 大肥鱼桌宠 · v0.2 版

双击本文件夹里的 **大肥鱼桌宠.exe** 即可启动。角色默认出现在屏幕右下角。

- **视线跟随鼠标**：鼠标在角色左侧时向左看，在右侧时向右看，在上方时抬头；在下方时低头，回到脸部附近或静止 5 秒后恢复正视。鼠标位置按角色当前大小和屏幕坐标换算，角色会平滑转头，并过滤短暂经过和边界抖动，不再循环张望、抬头。
- **自然待机**：持续轻微呼吸；每 3–5 秒随机眨眼一次，每次约 0.25 秒。各个朝向及转头途中均可眨眼，不切回正面。
- **左右张望**：默认开启。鼠标和交互安静约 20 秒后，偶尔看看两边再回正；移动鼠标、点击或拖动即可中断。右键 →「自主小动作（左右张望）」可关闭；「调节张望间隔…」可用滑块或秒数输入把等待时间设在 10–30 秒之间，默认 20 秒，每次实际等待在设定值附近小幅浮动。菜单或调节面板打开时暂停，结束后重新等待。
- **张望速度**：使用「调节动画速度…」中的独立「左右张望」倍率；1 倍时一轮约 4 秒。间隔控制多久发生一次，速度倍率控制转头快慢，两者独立保存。
- **拖动**：按住角色上的鼠标左键移动，角色会经过抬肩、提踵、离地过渡，再连续轻晃；松开后落地、站稳，并恢复待机。
- **点击**：轻点角色，会有一个轻微的缩放回应。
- **连续调节大小**：右键 →「调节大小…」，拖动滑块实时缩放，不再限制为三档。通常可在 30%～200% 之间任意调节；较小的屏幕会自动限制上限，让角色保持完整可见。
- **滚轮缩放**：将鼠标放在角色上，向上滚动放大、向下滚动缩小；支持高精度滚轮输入。调节面板中的百分比会同步更新。
- **恢复大小**：调节面板中点击「恢复 100%」。点「完成」或关闭面板均会记住当前大小。
- **右键**：切换置顶、回到屏幕右下角或退出。
- **找回**：双击任务栏通知区域里的桌宠图标，也可以右键该图标退出。

位置、大小和置顶状态自动保存在同目录的 `data/settings.txt`，下次启动时恢复。重复双击不会启动多个桌宠。

这是 Windows 原生透明窗口，角色素材已嵌入 EXE；不需要 Python、Node 或安装器。需要 Windows 自带的 .NET Framework 4.x。程序不联网、不设置开机启动。

## 文件

- `大肥鱼桌宠.exe`：直接运行，也可以单独复制到其他文件夹。
- `assets/pet-front.png`：透明正面角色素材。
- `assets/animations/v2/unified-sheet.png`：待机、拎起和起落共用的 16 姿势图集，统一灰蓝配色。
- `assets/animations/v2/atlas.json`：完整角色边界和动作编号。
- `assets/animations/v2/motion-fields.gz`：过渡轮廓的位移数据，已嵌入 EXE。
- `src/PetApp.cs`：窗口、拖动、点击和菜单逻辑。
- `src/PetAnimation.cs`、`src/PetMotionWarp.cs`：随屏幕刷新播放、起落状态和轮廓补间。
- `build.ps1`：使用 Windows 自带的 C# 编译器构建。
- 原始三视图 PNG：保留供以后添加侧面、背面或动作时使用。

修改源码或替换素材后，先退出桌宠，再在此文件夹运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
```

0.4 版取消了原先固定 30Hz 的动画定时器，改为跟随 WPF 合成刷新。悬空轻晃连续计算，起落使用独立过渡姿势和轮廓位移补间；实际刷新率仍由屏幕、系统负载与窗口合成情况决定。

素材通过内置 imagegen 工具处理，最终为较柔和的灰蓝色。透明素材保存在 `assets/pet-front.png`，完整处理提示词见 `assets/生成记录.md`。

0.4 版统一动画图集与完整生成提示词位于 `assets/animations/v2`。`动画预览.html` 保留为第一版动作参考，当前运行效果以新版 EXE 为准。

## 0.4 版验证（2026-09-18）

- 220 次原生 WPF 渲染检查通过，覆盖 30%、当前小数比例、100%、200% 大小及透明边界。
- 起身、悬空、落地、快速松手、落地中再次拖起的状态连续性检查通过。
- 待机和悬空使用同一张图集；同一区域发色采样的 RGB 均值距离约 2.8，全部姿势约在 4.4 以内。这是局部颜色一致性检查，不代表人物每一像素完全一致。
- 跨姿势时先对齐轮廓再补间，使用预乘 alpha 保留透明背景，避免简单叠图造成的重影和整体变透明。
- 桌宠运行不需要 Python 或 OpenCV。OpenCV 仅用于构建时分析位移数据。
- 如需测量本机合成回调，可带 `--animation-diagnostics` 参数启动；结果写入 `data/animation-performance.txt`，统计一次后停止记录。
- 新版启动后的 6.018 秒采样收到 519 次合成回调，平均约 86.24Hz，95% 回调间隔不超过 16.33ms，WPF 渲染等级为 2。此数值是应用合成回调统计，并非逐帧测量显示器实际呈现帧率。

## 本机验证（2026-09-17）

已实际启动透明窗口，验证鼠标拖动、单击缩放反馈、右键菜单退出、退出后重新启动恢复位置。最终配色已重新编译并启动，复验拖动后配置文件正确更新。多显示器之间的拖动尚未实机验证。

0.2 版已确认滑块能够设置并保存非预设的小数比例。滚轮放大一次后，角色高度从约 211.37 变为 221.94，再缩小一次回到原值；缩放时脚底位置基本保持不变。退出再启动后，精确高度 211.36986301369859 和位置成功恢复。

## 0.6 版更新

- 八方向跟随采用 0.28 秒平滑轮廓补间，方向边界有滞回，并过滤 0.12 秒以内的短暂经过。
- 眨眼与转头独立计算，使用对应朝向的闭眼帧，允许同时转头、眨眼。
- 鼠标静止按真实屏幕坐标计时，缩放窗口不会重新触发跟随。
- 新图集和位移数据位于 `assets/animations/v3`，旧素材保留。
- 眨眼间隔参考：https://eyewiki.aao.org/Eye_in_numbers 。3–5 秒为动画设计范围，不模拟个体生理差异。

## 0.7：原画连续跟随

停用 v3 新绘视角。所有待机朝向均直接采样 v2 原正视帧，使用小幅二维形变保留原色、头发留白与体型。眨眼仅替换原闭眼帧的眼睑区域，身体不切帧。该方法是轻幅二维朝向表现，不是重新绘制的大角度侧脸。

跟随从离散方向动画改成连续阻尼追随，目标变化时保留当前位置和速度，限制转头速度和加速度。鼠标快速扫动时不再排队播放姿势。仍支持所有方向、静止 5 秒回正、3–5 秒随机眨眼与拖拽起落。

验证包括原正视像素一致性、跟随时下半身像素不变、快速连续反向时速度与加速度限制、超时回正、眨眼节奏与多尺寸透明边界。原 v3 素材作为历史版本保留，不再嵌入当前程序。

## 0.8：重新绘制真实转头姿势

- 以 v2 原正视帧单独作为 imagegen 参考，生成八个真实转头视角及对应闭眼姿势。正视仍使用原图。
- 全组采用统一缩放比例与脚底基线，以原正视的宽高为校准基准，不再逐姿势按高度放大。
- 连续阻尼跟随保留，并用原正视与相邻两个绘制姿势进行轮廓对齐和连续混合；不再使用 0.7 的原图局部形变。
- 鼠标快速反向保留当前朝向和速度，眨眼与转头独立。
- 当前资源位于 `assets/animations/v4`，图集包含原正视、原起落和新视角，直接嵌入 EXE。
- 生成的新角度不可能与原图逐像素相同，已检查统一比例、配色、透明留白、闭眼角度和中间帧；保留原版本方便回退。
## 0.8.1：跟随响应和眨眼稳定性

转头阻尼响应加快约四分之一，保留原动作幅度和连续反向缓动。模拟单次转向到目标 90% 约 0.517 秒。
眨眼只从对应闭眼姿势中更新眼睑区域，头发、脸部外轮廓、身体及全部透明通道保持睁眼姿势不变，避免整张闭眼图切换引起跳变。
已检查所有 9 个朝向在 25%、50%、75%、100% 闭眼程度时，眼部以外像素和透明通道完全不变，并保留八方向、回正、快速反向和拖拽回归检查。

## 动画速度与数值输入

- 右键 →「调节动画速度…」：转头跟随、眨眼动作、待机呼吸、拎起过渡、落地过渡、悬空晃动、点击反馈可分别设置 **0.25–5 倍**。默认均为 1；想更跟手，可以先试转头 2 倍。
- 滑块即时生效；输入数值后按回车或移开焦点生效。支持小数；无效输入会提示并保留此前的有效设置。设置自动保存，支持恢复全部 1 倍。
- 「调节大小…」也支持直接输入百分比，例如 125.5。通常为 30–200%，具体上限随工作区大小调整。
- 倍率改变动画时间推进速度，不改变 5 秒静止回正计时或 3–5 秒眨眼间隔。眨眼倍率只控制闭眼、睁眼动作的快慢。
- 右侧三个朝向共享正视呆毛方向，头顶连接处渐变衔接；上、左上、右上重绘为略大的抬头幅度，并匹配相应闭眼姿势。眨眼仍只更新眼部。
- 当前嵌入 assets/animations/v5：保留 v4 的其余 28 帧，仅替换上方三个方向的睁眼/闭眼共 6 帧。构建源为 up-open-source.png、up-closed-source.png，位移数据由原生帧重新计算。
- 动画时钟与跟随算法在 src/PetAnimation.cs；倍率在 src/AnimationRates.cs；输入控件在 src/NumericSettingRow.cs；右键菜单在 src/PetApp.cs。
## 后台流畅度与轮廓修复

- 对当前进程显式关闭执行速度节流，保持 Normal 优先级；轮廓补间最多使用 4 个并行任务。用户已确认未选中时明显更流畅。
- 右侧三个姿势使用新的头顶局部修复素材，清理固定呆毛时残留的旧轮廓，面部和身体不替换。
- 鼠标方向落到完整的八方向姿势，转头期间仍平滑补间，停住后不再长期混合不同脸部或尾巴。方向边界带 7 度滞回，附近回正区域也有滞回。
- 动画倍率、大小、5 秒静止回正和眨眼间隔保留。496 次原生渲染检查及落定姿势、方向边界回归通过。

## 摸头回应与统一调速

- 不按鼠标，在她头顶左右轻扫约半秒到一秒，会将双手收在胸前，弯眼微笑、轻轻低头，再保持这个姿势左右小幅扭头撒娇，最后放手、睁眼回正；蹭动方向不受鼠标位置影响。回正后短暂停稳，再平滑恢复普通鼠标跟随。单次经过或停着不动不会触发；按住左键仍然拖动。回应结束后有 2 秒冷却。
- 右键 →「调节动画速度…」新增「左右张望」「摸头回应」，均可独立设置 0.25–5 倍，支持滑块、数值输入、自动保存和全部恢复 1 倍。
- 摸头 1 倍约 3.2 秒；倍率只影响动作，不改变手势识别阈值、冷却或张望频率。以后新增动作也统一加入这个面板。
- 摸头爱心是手绘蜡笔风：小爱心是干净的心形，大爱心带一圈星光点缀，两种混着飘起；「摸头爱心」倍率控制它们上升与淡出的快慢。
- 2026-09-19 新版摸头已制作接入；按用户要求未检查，等待用户自行体验反馈。

## 平滑调节大小（2026-09-19）

滚轮、大小滑块和百分比输入现在会平滑过渡到目标尺寸，脚底中心保持固定；点击或开始拖动会停在当前显示大小。原位置、大小、置顶、张望和动画倍率设置保留。缩放过程中暂不识别摸头手势，停止缩放后恢复。

## 张望间隔可调（2026-09-19）

右键 →「调节张望间隔…」：滑块 10–30 秒、按整秒吸附，也可以直接输入秒数，回车或移开焦点生效，默认 20 秒并自动保存。原「张望频率（少／标准／多）」取消；旧设置会自动换算成新的秒数（少 → 30 秒、标准 → 20 秒、多 → 15 秒），其余设置不变。间隔是真实时间，不受「左右张望」倍率影响，倍率只控制转头快慢。

## 和我说话（DeepSeek 接口，2026-09-19）

- 右键 →「和我说话…」打开对话窗口：输入内容后按回车或点「发送」，大肥鱼会回你一句；窗口里能翻看历史，也可以「清空记录」重新开始。
- 窗口里**她那一侧的回复也用手绘气泡显示**（尾巴朝左），和桌面上弹出的气泡是同一套素材；你那一侧是浅蓝圆角气泡。窗口顶部不再显示大标题，消息更贴边。
- 第一次使用先点窗口里的「接口设置」：默认接口地址 `https://api.deepseek.com/v1`、模型 `deepseek-chat`，把 DeepSeek 的 API Key 粘贴到「API Key」后保存即可。换其它服务商时改接口地址和模型名就行。
- API Key 用 Windows 自带的 DPAPI 加密后只存在本机 `data\chat-key.dat`，不会写进 `settings.txt`，也不会被 git 跟踪；接口地址和模型名存在 `data\chat-config.txt`。想清掉 Key 就在设置面板点「清空 Key」。
- 聊天记录保存在 `data\chat-history.jsonl`，一行一条，可以自己打开查看或删除；点「清空记录」会删除该文件。
- 没填 Key、断网或接口报错时，她会改用本地台词回应，并在窗口底部说明原因，不会卡住；单次请求最多等 60 秒。
- 目前是一条完整回复（不是逐字打字）。语音朗读、语音输入和"说话时带动作"按计划放在后续版本。

## 动画色调统一（2026-09-19）

各动画帧（正视、上下左右各朝向、眨眼、拎起落地、摸头）的亮度已统一到正视的水平，之前跟随鼠标转头、进入摸头时出现的明暗跳变会明显减少。统一只调亮度、不改色相：肤色、发色和布料配色保持原样。生成流程会在每次重做素材后自动按正视重新对齐，所以以后再单独改某一张素材也不会让色调跑偏。

## 说话气泡（2026-09-19）

- 大肥鱼回话时会在**她的侧边**弹出对话气泡，不需要打开「和我说话…」窗口也能看到她说了什么。文字越长显示越久（3–14 秒），之后自动淡出。文字越多气泡越大（宽和高都跟着长），一句话就是薄薄一条。
- 气泡外观来自你提供的手绘参考图：纸纹奶白填充、浅蓝双层描边、左上小笔触、右上爱心、右下小鲸鱼，底部有尾巴。九宫格绘制，四角和装饰不随文字拉伸变形。
- 文字在气泡里**居中**显示；尾巴和小鲸鱼在右下角保持原始形状、和气泡描边连成一体（镜像到另一侧时同样保持闭合）。
- **出现在哪一侧**：看她离桌面哪边边缘更远——她靠左半屏就把气泡放在她右边，靠右半屏就放在左边；气泡始终留出间隙，不会压住她。
- **尾巴固定在靠她那一侧的底角**（气泡在她左边时尾巴在右下、在她右边时在左下），不再左右滑动。
- 气泡会尽量贴近她（只留一点点间隙），并且始终在她侧边、不挡住她。
- 点一下气泡会直接打开对话窗口，方便接着聊；右键菜单新增「显示对话气泡」开关（默认开启，设置会保存）。
- 等待回复时会先显示「……」，回复到达后立刻替换成内容；没有填 API Key 或网络失败时，气泡里显示本地台词，原因写在对话窗口底部。
- 气泡会跟着角色移动和缩放，位置保持在角色上方，不会挡住角色本体；如果角色贴到屏幕最上方，气泡会被限制在屏幕范围内。

## 主动搭话（2026-09-20）

- 她会自己开口，不需要你先发消息：启动几秒后先打个招呼（按早上／下午／晚上／深夜换台词），之后每隔一段时间主动说一句闲话，默认 30 分钟一次。
- 间隔可调：右键 →「搭话间隔…」，滑块按 5 分钟一档，范围 5–90 分钟；每次实际间隔在设定值附近浮动（0.6–1.4 倍），所以不会精确到秒地重复。
- 大约 45 分钟没和她互动，她会提醒你起来动一动、喝口水（两次提醒之间至少隔 1 小时）。两次主动说话之间至少隔 3 分钟，每天随机闲聊最多 16 次，避免打扰。
- 摸头、点击、拎起、放下的时候她也会接一句；点她不是每次都说话。
- 台词全部是本地预设的，不联网、不花钱，断网也能用。右键「主动搭话」可以整体关闭。
- 她说的话显示在头顶气泡里，几秒后自动淡出；点气泡可以打开对话窗口接着聊。
